# Archive Index — 2026-08

| Feature | Archived | Match Rate | Iter | 시간 | Note |
|---------|:--------:|:---:|:---:|:---:|------|
| [first-semiconductor-primer](./first-semiconductor-primer/) | 2026-08-09 | 96% | 0 | 2일(2세션) | 입문서 「처음 배우는 반도체」(기쿠치 마사노리 菊地正典, OCR 4,180행) **9장 88항목 + COLUMN 9 = 97모듈 전량 재구성** — 사이트 **13번째 자료원**(order 13, `kind: 'primer'` 신설, accent `school`), 본문 **173,155자**(항목 88 평균 1,837자 / COLUMN 9 평균 1,274자). 사용자 요구 3가지("모든 챕터 누락 없이"·"이전 자료들과 연계"·"알기 쉽게 풀어쓴")를 전부 충족. **입도 결정을 사용자가 뒤집은 사이클** — 오케스트레이터는 20그룹을 권했으나 사용자가 **항목 단위 88모듈**을 택했고, 이 선택이 "빼먹지 말라"는 요구를 입도 자체로 보장해 누락 0을 구조적으로 달성. **Step 0 OCR 목차 복원이 최대 난관**: 4·5장 목차 붕괴 + 항목 번호 헤딩 68개 누락 상태에서, 페이지 마커(`<!-- page: N -->`)와 원서 쪽수 간 **오프셋 −4가 전 구간 일정**함을 6개 지점(002·024·031·034·043·086)에서 교차 검증해 97항목의 번호·제목·쪽수·라인스팬을 기계적으로 확정. 제목 소실 2건 처리(057 내용 기반 부여 / 062 목차 복원) + Do 중 발견한 OCR 오류 3건(035 표 열제목·047 레벨 할당·col-7 ISO 14644-1)까지 **전 5건을 각 페이지 출처란에 근거와 함께 명기**. **신설 통제 어휘 topic 5종**(device-physics·logic-circuit·memory-device·ic-design·advanced-node, 상한 ≤22→≤28 재설정) — Design 검토에서 `lookupRelated`의 **자기 참조 제외가 source 단위**임을 발견(V-2), 상대편 태깅 없이는 5종 전부 연결 0이 되어 **FR-8로 전량 철회될 뻔한 위기**를 최소 소급 태깅 4파일(`ncs-semi`·`hs-semicon-basics`·`hs-basic-tech-2`·`hs-photo-etch`)로 해소 → 전 topic 역방향 연결 확보(device-physics 7·ic-design 6·logic-circuit 3·advanced-node 2·memory-device 1). cross-link **291건·모듈당 평균 3.0·연결 0 모듈 0개**. 파일럿 3모듈 게이트 후 **5배치**(B1 3·4장 26 / B2 9장 12 / B3 1·2장 24 / B4 5장 6 / B5 6·7·8장 26) 완주. **Do 단계 렌더 정정 2건**: `kind: 'book'`의 라벨 '학술서'가 카드·footer에 오표기되어 `'primer'`('입문서'/'항목') 신설(V-4), `accent: 'book'`이 홈에서 **'위험 · 왜 위험한가'** 군집에 오배치되어 `'school'`로 정정 + 카피를 '학교에서 배우는 그대로' → **'기초부터 차근차근'** 으로 확장(V-5). **9장 시점차 처리 패턴 확립** — 2010년경 원서의 '미래 기술' 전망을 그대로 옮기고 이후 실제 전개(FinFET·EUV·High-k 양산화 / 450mm 미도입 / PRAM 사업 종료)를 별도 절로 병기. 공용 라우트·통합 로더 100% 재사용으로 **신규 라우트·로더 파일 0**, 기존 12자료원 본문 수정 0(허용 예외 `_links.json` 4파일만), 번들 무변(First Load JS 102kB·모듈 라우트 1.72kB). 사고 1건: **부등호 일괄 치환 정규식이 `<strong>` 태그의 닫는 괄호까지 `&gt;`로 바꿔 22파일 50건 태그 파손** → 태그 패턴 역치환으로 전량 복구, 이후 배치는 작성 시 `&lt;` 직접 사용 + 등록 전 전수 사전 점검으로 무사고(교훈: MDX에서 `>`는 안전하고 `<` 뒤 숫자만 문제). 검출 성과: 4중 정합성 검증(`sections`↔`REGISTRY`↔MDX↔`_links`)이 **`043-dram` sections 소실**(Edit old_string이 043 전체를 042로 덮어씀)을 적발 — 이 검증이 없었으면 빌드도 통과하고 조용히 누락될 결함. **자기 검증이 4번 틀렸다**(전부 정정 기록): ①신규 로더 개수 하드코딩 ②`전면\n재작성` 줄바꿈 매칭 실패 ③topics 정규식 누락 ④**Match Rate 산출 자체** — 판권(Design §7 항목)을 매트릭스 50항목 안의 ❌로 이중 계상해 ✅를 임의 차감(94.0%), 그룹 소계 합 ✅48+⚠2=50이 정확해 **처음부터 96.0%**였음. 남은 ⚠2건은 분량 초과(항목 최대 2,341자·COLUMN 4개)로 **무수정 승인** — 덜어 내면 표·비교가 깨져 품질이 오히려 떨어짐, Design §5.1 수치 기준을 실측 분포로 현행화 권고. **판권 게이트(D-1) 종결**: 원문 스캔본에 판권 페이지가 없어 역자·출판사·발행연도 끝까지 미확인이나, 사용자 결정으로 **로그인 게이트 뒤 비공개 개인 학습용 운영**이라 공개 배포가 성립하지 않아 확인 불요 → `publisher` 필드 제거(이때 `disclosureFor`의 no-publisher 분기가 13자료원 중 처음 실행되며 괄호 중첩 발생 → `attribution`을 `'기쿠치 마사노리 菊地正典'`으로, 분기를 `(attribution 지음)`으로 함께 정리). **잔여 조건 — 공개 전환 시 판권 확인이 다시 게이트**. 저작권: 원문 이미지 336개 **전면 미사용**, 문장 전면 재작성(3단 레이어의 `deep`도 요약 서술), 출처 표기 97/97. 게이트 전부 통과(typecheck·lint 0, build **379페이지** SSG, cross-link 어휘 검증 통과, quotes.json 무변). **PR [#27](https://github.com/DrunkenZealnut/semiconductor-academy/pull/27)** · 커밋 5(feat `e18cd4c` 114파일 +16,352/−1,401 → fix `8abffa2` CodeRabbit 반영 → chore `0391cca` 판권 종결 → docs `f773bea` 보고서 → docs `c22c73d` 아카이브). CodeRabbit 리뷰 **16건 중 15건 반영**: 명백한 오류 2건(**087 EOT 식 분모 `ε₀`→`ε_SiO₂`**, col-3 ENIAC 크기는 패널 한 대 치수 → 전체 점유 면적·무게 병기), 범위 정정 4건(050 "글로벌 공통 표준"→**공정별 PDK**, 058 FinFET은 SOI 필수 아님→벌크/SOI 병존, 086 ReRAM을 CER로 한정하지 않고 **전도 필라멘트 모델** 병기, 046 CTF 병기·026 PLD/FPGA 맥락), 설명 보강 9건. **1건 보류** — COLUMN에 `LayeredExplain` 추가는 Design §5.1의 명시적 결정("COLUMN은 단일 서술 + Callout")이라 근거를 PR 코멘트로 회신. ⚠️ Claude 초벌(기술 수치·서술 사람 검수 권장). Branch `feat/first-semiconductor-primer` → `main` |
| [cert-equip-maintenance](./cert-equip-maintenance/) | 2026-08-09 | 98~99% | 2 | 1 세션 | 국가기술자격 수험서 「반도체설비보전기능사 필기」(김종주 외, 에듀크라운, OCR 17,860줄·518쪽) 이론편 Part 01~10 **13모듈 전량 재구성** — 사이트 **14번째 자료원**(신규 `SourceKind: 'exam-prep'`, order 14, accent `standard`=직무 클러스터; first-semiconductor-primer와의 병합 시 order 13 충돌로 14 배정). 사용자 요청("관련된 내용들을 다른 자료들에서 최대한 가져와서 자세하고 쉽게 커멘트")에 정면 대응 — 기존 자료원 전체로 향하는 **인라인 SourceRef/ChapterRef 커멘트 87개+**(모듈당 평균 6.7개, 목표 4개 초과), 출제기준 연결 **시험 포인트 Callout 50개**(모듈당 2~4개). 원문을 22개 슬라이스로 분할해 **Sonnet 서브에이전트 13개 병렬 제작**, cross-link `_links.json` 태깅으로 기존 daegu-hs-process·책 챕터 페이지에도 수험서가 역방향 노출됨을 빌드 산출물에서 실증. gap-detector 1차 **97%** → High 1(공유압 회로 기호 scope 누락)·Med 2(안전관리 조직 미서술·SafetyDisclaimer 누락)·Low 2 **즉시 보수** → pdca-iterator **Evaluator-Optimizer 2회 반복**(태그 변별력 검토 후 의도적 무변경 1건 + scope 보강 4모듈 + cross-link 태그 정정 2건) → 최종 추정 **98~99%**. 잔여 갭 1건(Low, photo-process 노광 광원 파장값 — 원문에 텍스트 근거 없어 "확신 없는 수치는 버린다" 원칙상 의도적 보류, 구조적 한계). 저작권: daegu 최보수 원칙 일괄(원문 문장·이미지·문제 재현 0, 개념·수치만 근거, 출처 표기). 게이트 전부 통과(typecheck·lint 0, build 295페이지 SSG 2회 독립 재확인, cross-link 무오류). 기존 코어(홈 PerspectiveCatalog·`[source]/page.tsx`·`[source]/[module]/page.tsx`·extract-quotes) 무수정. **main 병합 시 first-semiconductor-primer와 order 13 충돌 발견**(양쪽이 같은 사이클에서 독립적으로 order 13을 확정) — CERT_EQUIP_MAINT를 order 14로 재배정해 해소, cross-link.json 재생성으로 정합 확인. Branch `DrunkenZealnut/새로운책등록`, 커밋 완료(main 병합 후 PR 예정) |
| [diagram-component-set](./diagram-component-set/) | 2026-08-10 | 93.6% | 1 | 2일 | **사이트 최초의 자체 제작 도해 세트** — 그때까지 14개 자료원 이미지 425개가 전부 원문 스캔이었고 자체 SVG는 `og-default.svg` 하나뿐, MDX 인라인 `<svg>`는 0건이었다. 즉 백로그 표현("자체 다이어그램 **확대**")과 달리 실제로는 **첫 도입**이었다. 도해 컴포넌트 **12종** 신설(`src/components/diagram/`, 16파일) + first-semiconductor **97/97 모듈**에 **140 인스턴스**(주 97 + 보조 43) 적용, 배치표 미이행 0건. 전부 **서버 컴포넌트 정적 SVG**라 **First Load JS 102kB 무증가**·모듈 라우트 1.72kB/125kB 무증가 — 12종 × 140 인스턴스를 넣고도 번들 회귀 0. **8종 → 12종 확대(V-1)**: Step 0 전수 배치에서 8종으로 주 도해가 성립하지 않는 **22모듈**이 드러남(분류 트리 7·특성 곡선 5·기능 블록도 5·원자 격자 4·연표 4) → `CircuitSchematic`을 **`NodeGraph`로 일반화**해 회로도+블록도를 흡수하고 `TreeBranch`·`CurvePlot`·`LatticeDiagram`·`Timeline` 추가. `design-validator`가 **4건 적발**(V-2): 파일럿 019·029가 **2차 예정인 `NodeGraph`를 요구**하는 실행 불가 순서, 최다 사용 2위 `CompareCards`가 파일럿에서 미검증, §1.2 `col-3` 누락으로 커버율이 실제 96/97, §1.3 집계가 6개 항목 오류(주 도해 합 100 — 산술상 불가) → `NodeGraph` 1차 승격·파일럿 5모듈 확대로 해소. **`idPrefix` 규약**: 서버 컴포넌트라 `useId()`를 못 써 전역 `id`를 만드는 2종(`LayerStack`의 `<pattern>`·`NodeGraph`의 `<marker>`)만 필수로 좁힘(V-3) — 한 페이지 2도해(019·058)에서 충돌 0 실증. **원문 근거 규약이 9건을 되돌림**: col-8 욕조곡선 부재(배정 자체 교체·V-4), 001 저항률 상한 `1e14`, 087 High-k 유전율(원서는 재료명만), col-2·col-3 연도, 078 웨이퍼 지름 세대, 047 V_TH 정성값, 050 노드 이력, col-7 클래스 등급 — 특히 001·087은 **다른 표에서 값을 끌어오거나 상식으로 채운** 경우로, 도해 작업 특유의 함정임이 드러나 `usage.md`에 명문화. **자수 측정 로직 결함 발견(V-5)**: `<[^>]+>` 정규식이 도해 JSX 블록을 통째로 삼키는데 내부에 `<strong>`이 있으면 거기서 끊겨, 같은 작업인데 011은 ±0·018은 +141로 잡혔다 → 줄 단위 제거로 교정하고 선행 사이클 §5.1에 예외 문안까지 역반영. Check 1차 **78.1%**(Gap High 4·Med 3·Low 4) → 사용자 결정 "보조를 채운다"로 26건 전량 적용 + 배치표 12건 중 7건 이행·5건 정정(V-6) → **93.6%**(`pdca-iterator` 미사용, 수동 Act). **선행 사이클 문서 2건 갱신**: §5.2를 **T1 정밀 / T2 패턴 / T3 시각 강화 3단 판정**으로 재정의(V-7 — 초판의 "기존 컴포넌트로 표현 가능" 조건은 인프라 0이던 시절의 억제책이었다), §5.1 분량 기준 현행화(항목 1,200~2,300 / COLUMN 600~1,600) + 도해 자수 예외. **PR [#29](https://github.com/DrunkenZealnut/semiconductor-academy/pull/29)** · 커밋 5(feat `8db9620` 127파일 → chore `acf73dd` → merge `16e352f` main 병합 충돌 해소 → docs `afd316d` → docs `b928709` 보고서+리뷰 반영) · merge commit `3bb46b5` · +5,709/−342. **main 병합 충돌**: 같은 시기 PR #28(cert-equip-maintenance)이 머지돼 `pdca-status.json` 충돌 → 한쪽을 버리지 않고 features·history(450건)·phaseHistory를 수동 병합. CodeRabbit **1차 3건 전량 반영**: 상태 파일 보조 수량 10/36→43/43, `commit` 필드 해시-only 분리, **CLAUDE.md 배포 계약 정정**(GitHub Pages·`output:'export'` → Vercel + 미들웨어가 정적 export를 막는 이유 명기 — Do 중 이미 발견해 Plan에만 기록했던 것을 본문까지 수정). ⚠️ **미완: 게이트 G-1(라이트·다크·375px 실렌더)·G-3(시각 언어 대조) 육안 미확인** — 로그인 게이트 + Chrome 확장 미연결로 자동 확인 불가, 정적 대체 검증(하드코딩 색 0·본문 가로 스크롤 경로 없음·SVG id 충돌 0)만 수행. Claude 초벌(도해 기술 정확성 사람 검수 권장). Branch `feat/diagram-component-set` → `main` |

---

## 기록 정책

- 아카이브 후에도 `.bkit/state/pdca-status.json`의 `features`에는 항목을 남기고 `phase: "archived"` + `archivedTo`를 기록한다 (이 저장소 관례 — 2026-05 이후 전 사이클 동일).
- Plan/Design/Analysis/Report 문서는 `docs/archive/YYYY-MM/{feature}/` 폴더로 이동한다.
- **지속 참조 자산은 아카이브하지 않는다.** `diagram-component-set`의 `usage.md`(12종 사용 규약)와 `baseline.md`(97모듈 자수 고정값)는 `docs/02-design/features/`에 남긴다 — CLAUDE.md와 선행 사이클 Design §5.1·§5.2가 이 경로를 참조하고, 후속 자료원 저작자가 계속 봐야 하는 살아 있는 문서이기 때문이다.

## 후속 백로그 (first-semiconductor-primer 파생)

- ~~**Design §5.1 분량 기준 현행화**~~ → ✅ **2026-08-09 완료**. 항목 1,200~2,000 → **1,200~2,300**, COLUMN 600~1,200 → **600~1,600**. 갱신 과정에서 수치 외 2건이 더 드러나 함께 반영했다: ① 설계는 **Easy(600~1,000자)를 주력**으로 봤으나 실물은 `LayeredExplain`이 도입부(Easy 평균 168자)이고 **`##` 본문 섹션이 주력(평균 1,173자)** — 설계 표에 아예 없던 층이라 행을 신설했다. ② report(평균 1,837·최대 2,341)와 analysis(평균 1,753·최대 2,274)의 수치 불일치는 **측정 기준 차이**(마크다운 기호 포함 여부)여서, §5.1에 측정 방법을 **공백·마크업 제외**로 못 박았다. 그 기준에서 88항목 전량이 새 범위 안. (Design §0 V-6 · §5.1 / Plan §4.1 주석 / Report §4.1·§8.2)
- **공개 전환 시 판권 재확인**: 현재 종결은 **비공개 운영이 전제**다. 로그인 게이트를 해제하면 역자·출판사·발행연도 확인이 다시 공개 전 게이트가 된다 (Report §4.2).
- **자체 다이어그램 확대 검토**: FR-9는 "필요 최소" 기준으로 충족(표·목록으로 대체)했으나, 소자 구조·공정 흐름은 도해가 있으면 이해도가 오른다. 특히 2장(소자)·7장(전공정).
- **입문 → 심화 동선 재검토**: 97모듈 입문 계층이 새로 생겼으므로 홈의 "여기서 시작" 진입점과 `RoleQuickEntry` pill 구성을 다시 볼 여지. `mainpage-restructure`(2026-07) 이후 자료원이 12→13개로 늘었다(이후 cert-equip-maintenance 병합으로 14개).
- **기술 서술 사람 검수**: CodeRabbit이 16건을 잡아낸 데서 보이듯 원서(2010년경) 서술에는 현재 기준과 어긋나는 대목이 더 있을 수 있다. 특히 9장(최첨단)과 4장(메모리)은 이후 전개가 큰 분야다.
- **Gap 판정 스크립트에 양성 대조군**: 이번 사이클에서 자기 검증이 4번 틀렸다. 판정 스크립트에 의도적으로 틀린 입력을 함께 넣어 **스크립트 자체를 검증**하는 절차가 필요하다. Match Rate는 그룹 소계 합과 총계를 교차 검증한다.

## 후속 백로그 (cert-equip-maintenance 파생)

- **photo-process 노광 광원 파장값 보강**: g-line/i-line/KrF/ArF 등 구체 파장 수치가 원문 슬라이스에 텍스트 근거 없이 그림 캡션으로만 존재해 미보강(창작 금지 원칙). 원본 PDF 재확인 또는 타 신뢰 자료로 보강 가치 있음.
- **태그 변별력 재검토**: automation-plc·electrical-facility·environment-management 3개 모듈이 `engineering-controls` 단일 태그라 cross-link 관련자료 집합이 유사. 본문이 확장되면(백로그 하단) 2번째 축 재검토.
- **첫 자료원(입문서) ↔ 수험서 상호 연결 검토**: first-semiconductor-primer(13번째, 개념 입문)와 cert-equip-maintenance(14번째, 자격증 실무)가 같은 사이클에서 독립적으로 병합돼 서로를 모르는 상태 — cross-link 상호 태깅 여지 있음(후속 사이클).

## 후속 백로그 (diagram-component-set 파생)

- **G-1·G-3 육안 확인**: 파일럿 5모듈(017·059·019·029·063)을 라이트·다크·375px로 실제 렌더 확인. 로그인 게이트 뒤라 사람만 가능하며, 이것이 이 사이클의 유일한 미완 게이트다.
- **`LayerStack.orientation('band')` 미구현**: 007·008 밴드 다이어그램이 일반 적층과 같은 렌더다. Design §2.2에 시그니처만 있고 구현이 없다 — 구현하거나 시그니처를 삭제한다.
- **`Timeline.span` 미구현**: 같은 성격.
- **도해 글자 크기 상대 단위**: Design §3.2가 `em`을 요구했으나 구현은 viewBox 절대값(`DIM.font: 13`)이다. `FontSizeToggle`로 본문을 키워도 도해 라벨은 그대로여서 비율이 어긋난다. viewBox 안에서 `em`을 쓰면 도해 폭과 글자가 따로 노는 문제가 있어 절대값을 택했으나, 그 판단이 Design에 기록되지 않았다.
- **FR-8 `altTable` 판정 확정**: 9 인스턴스만 제공하고 나머지는 표를 지우지 않고 병치했다(정보 손실 0). "표 병치"를 대안 이행으로 Design에 명문화할지, altTable을 전 도해에 채울지 결정 필요.
- **tokens 우회 2건**: `NodeGraph.KIND_FILL` 3종과 `LatticeDiagram`의 slate 리터럴이 `TONE`을 참조하지 않는다(문자열은 동일).
- **`<desc>` 0건**: Design §4가 `<title>`+`<desc>`를 요구했으나 `<title>`만 있다.
- **도해 세트의 타 자료원 확대**: 12종은 공용 자산으로 만들었으나 적용은 first-semiconductor뿐이다. daegu-hs-process·NCS·OSHA에도 같은 시각 언어를 쓸 여지가 있다.
