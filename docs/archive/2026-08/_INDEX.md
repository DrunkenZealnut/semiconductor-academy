# Archive Index — 2026-08

| Feature | Archived | Match Rate | Iter | 시간 | Note |
|---------|:--------:|:---:|:---:|:---:|------|
| [first-semiconductor-primer](./first-semiconductor-primer/) | 2026-08-09 | 96% | 0 | 2일(2세션) | 입문서 「처음 배우는 반도체」(기쿠치 마사노리 菊地正典, OCR 4,180행) **9장 88항목 + COLUMN 9 = 97모듈 전량 재구성** — 사이트 **13번째 자료원**(order 13, `kind: 'primer'` 신설, accent `school`), 본문 **173,155자**(항목 88 평균 1,837자 / COLUMN 9 평균 1,274자). 사용자 요구 3가지("모든 챕터 누락 없이"·"이전 자료들과 연계"·"알기 쉽게 풀어쓴")를 전부 충족. **입도 결정을 사용자가 뒤집은 사이클** — 오케스트레이터는 20그룹을 권했으나 사용자가 **항목 단위 88모듈**을 택했고, 이 선택이 "빼먹지 말라"는 요구를 입도 자체로 보장해 누락 0을 구조적으로 달성. **Step 0 OCR 목차 복원이 최대 난관**: 4·5장 목차 붕괴 + 항목 번호 헤딩 68개 누락 상태에서, 페이지 마커(`<!-- page: N -->`)와 원서 쪽수 간 **오프셋 −4가 전 구간 일정**함을 6개 지점(002·024·031·034·043·086)에서 교차 검증해 97항목의 번호·제목·쪽수·라인스팬을 기계적으로 확정. 제목 소실 2건 처리(057 내용 기반 부여 / 062 목차 복원) + Do 중 발견한 OCR 오류 3건(035 표 열제목·047 레벨 할당·col-7 ISO 14644-1)까지 **전 5건을 각 페이지 출처란에 근거와 함께 명기**. **신설 통제 어휘 topic 5종**(device-physics·logic-circuit·memory-device·ic-design·advanced-node, 상한 ≤22→≤28 재설정) — Design 검토에서 `lookupRelated`의 **자기 참조 제외가 source 단위**임을 발견(V-2), 상대편 태깅 없이는 5종 전부 연결 0이 되어 **FR-8로 전량 철회될 뻔한 위기**를 최소 소급 태깅 4파일(`ncs-semi`·`hs-semicon-basics`·`hs-basic-tech-2`·`hs-photo-etch`)로 해소 → 전 topic 역방향 연결 확보(device-physics 7·ic-design 6·logic-circuit 3·advanced-node 2·memory-device 1). cross-link **291건·모듈당 평균 3.0·연결 0 모듈 0개**. 파일럿 3모듈 게이트 후 **5배치**(B1 3·4장 26 / B2 9장 12 / B3 1·2장 24 / B4 5장 6 / B5 6·7·8장 26) 완주. **Do 단계 렌더 정정 2건**: `kind: 'book'`의 라벨 '학술서'가 카드·footer에 오표기되어 `'primer'`('입문서'/'항목') 신설(V-4), `accent: 'book'`이 홈에서 **'위험 · 왜 위험한가'** 군집에 오배치되어 `'school'`로 정정 + 카피를 '학교에서 배우는 그대로' → **'기초부터 차근차근'** 으로 확장(V-5). **9장 시점차 처리 패턴 확립** — 2010년경 원서의 '미래 기술' 전망을 그대로 옮기고 이후 실제 전개(FinFET·EUV·High-k 양산화 / 450mm 미도입 / PRAM 사업 종료)를 별도 절로 병기. 공용 라우트·통합 로더 100% 재사용으로 **신규 라우트·로더 파일 0**, 기존 12자료원 본문 수정 0(허용 예외 `_links.json` 4파일만), 번들 무변(First Load JS 102kB·모듈 라우트 1.72kB). 사고 1건: **부등호 일괄 치환 정규식이 `<strong>` 태그의 닫는 괄호까지 `&gt;`로 바꿔 22파일 50건 태그 파손** → 태그 패턴 역치환으로 전량 복구, 이후 배치는 작성 시 `&lt;` 직접 사용 + 등록 전 전수 사전 점검으로 무사고(교훈: MDX에서 `>`는 안전하고 `<` 뒤 숫자만 문제). 검출 성과: 4중 정합성 검증(`sections`↔`REGISTRY`↔MDX↔`_links`)이 **`043-dram` sections 소실**(Edit old_string이 043 전체를 042로 덮어씀)을 적발 — 이 검증이 없었으면 빌드도 통과하고 조용히 누락될 결함. **자기 검증이 4번 틀렸다**(전부 정정 기록): ①신규 로더 개수 하드코딩 ②`전면\n재작성` 줄바꿈 매칭 실패 ③topics 정규식 누락 ④**Match Rate 산출 자체** — 판권(Design §7 항목)을 매트릭스 50항목 안의 ❌로 이중 계상해 ✅를 임의 차감(94.0%), 그룹 소계 합 ✅48+⚠2=50이 정확해 **처음부터 96.0%**였음. 남은 ⚠2건은 분량 초과(항목 최대 2,341자·COLUMN 4개)로 **무수정 승인** — 덜어 내면 표·비교가 깨져 품질이 오히려 떨어짐, Design §5.1 수치 기준을 실측 분포로 현행화 권고. **판권 게이트(D-1) 종결**: 원문 스캔본에 판권 페이지가 없어 역자·출판사·발행연도 끝까지 미확인이나, 사용자 결정으로 **로그인 게이트 뒤 비공개 개인 학습용 운영**이라 공개 배포가 성립하지 않아 확인 불요 → `publisher` 필드 제거(이때 `disclosureFor`의 no-publisher 분기가 13자료원 중 처음 실행되며 괄호 중첩 발생 → `attribution`을 `'기쿠치 마사노리 菊地正典'`으로, 분기를 `(attribution 지음)`으로 함께 정리). **잔여 조건 — 공개 전환 시 판권 확인이 다시 게이트**. 저작권: 원문 이미지 336개 **전면 미사용**, 문장 전면 재작성(3단 레이어의 `deep`도 요약 서술), 출처 표기 97/97. 게이트 전부 통과(typecheck·lint 0, build **379페이지** SSG, cross-link 어휘 검증 통과, quotes.json 무변). **PR [#27](https://github.com/DrunkenZealnut/semiconductor-academy/pull/27)** · 커밋 5(feat `e18cd4c` 114파일 +16,352/−1,401 → fix `8abffa2` CodeRabbit 반영 → chore `0391cca` 판권 종결 → docs `f773bea` 보고서 → docs `c22c73d` 아카이브). CodeRabbit 리뷰 **16건 중 15건 반영**: 명백한 오류 2건(**087 EOT 식 분모 `ε₀`→`ε_SiO₂`**, col-3 ENIAC 크기는 패널 한 대 치수 → 전체 점유 면적·무게 병기), 범위 정정 4건(050 "글로벌 공통 표준"→**공정별 PDK**, 058 FinFET은 SOI 필수 아님→벌크/SOI 병존, 086 ReRAM을 CER로 한정하지 않고 **전도 필라멘트 모델** 병기, 046 CTF 병기·026 PLD/FPGA 맥락), 설명 보강 9건. **1건 보류** — COLUMN에 `LayeredExplain` 추가는 Design §5.1의 명시적 결정("COLUMN은 단일 서술 + Callout")이라 근거를 PR 코멘트로 회신. ⚠️ Claude 초벌(기술 수치·서술 사람 검수 권장). Branch `feat/first-semiconductor-primer` → `main` |
| [cert-equip-maintenance](./cert-equip-maintenance/) | 2026-08-09 | 98~99% | 2 | 1 세션 | 국가기술자격 수험서 「반도체설비보전기능사 필기」(김종주 외, 에듀크라운, OCR 17,860줄·518쪽) 이론편 Part 01~10 **13모듈 전량 재구성** — 사이트 **14번째 자료원**(신규 `SourceKind: 'exam-prep'`, order 14, accent `standard`=직무 클러스터; first-semiconductor-primer와의 병합 시 order 13 충돌로 14 배정). 사용자 요청("관련된 내용들을 다른 자료들에서 최대한 가져와서 자세하고 쉽게 커멘트")에 정면 대응 — 기존 자료원 전체로 향하는 **인라인 SourceRef/ChapterRef 커멘트 87개+**(모듈당 평균 6.7개, 목표 4개 초과), 출제기준 연결 **시험 포인트 Callout 50개**(모듈당 2~4개). 원문을 22개 슬라이스로 분할해 **Sonnet 서브에이전트 13개 병렬 제작**, cross-link `_links.json` 태깅으로 기존 daegu-hs-process·책 챕터 페이지에도 수험서가 역방향 노출됨을 빌드 산출물에서 실증. gap-detector 1차 **97%** → High 1(공유압 회로 기호 scope 누락)·Med 2(안전관리 조직 미서술·SafetyDisclaimer 누락)·Low 2 **즉시 보수** → pdca-iterator **Evaluator-Optimizer 2회 반복**(태그 변별력 검토 후 의도적 무변경 1건 + scope 보강 4모듈 + cross-link 태그 정정 2건) → 최종 추정 **98~99%**. 잔여 갭 1건(Low, photo-process 노광 광원 파장값 — 원문에 텍스트 근거 없어 "확신 없는 수치는 버린다" 원칙상 의도적 보류, 구조적 한계). 저작권: daegu 최보수 원칙 일괄(원문 문장·이미지·문제 재현 0, 개념·수치만 근거, 출처 표기). 게이트 전부 통과(typecheck·lint 0, build 295페이지 SSG 2회 독립 재확인, cross-link 무오류). 기존 코어(홈 PerspectiveCatalog·`[source]/page.tsx`·`[source]/[module]/page.tsx`·extract-quotes) 무수정. **main 병합 시 first-semiconductor-primer와 order 13 충돌 발견**(양쪽이 같은 사이클에서 독립적으로 order 13을 확정) — CERT_EQUIP_MAINT를 order 14로 재배정해 해소, cross-link.json 재생성으로 정합 확인. Branch `DrunkenZealnut/새로운책등록`, 커밋 완료(main 병합 후 PR 예정) |
| [diagram-component-set](./diagram-component-set/) | 2026-08-10 | 93.6% | 1 | 2일 | **사이트 최초의 자체 제작 도해 세트** — 그때까지 14개 자료원 이미지 425개가 전부 원문 스캔이었고 자체 SVG는 `og-default.svg` 하나뿐, MDX 인라인 `<svg>`는 0건이었다. 즉 백로그 표현("자체 다이어그램 **확대**")과 달리 실제로는 **첫 도입**이었다. 도해 컴포넌트 **12종** 신설(`src/components/diagram/`, 16파일) + first-semiconductor **97/97 모듈**에 **140 인스턴스**(주 97 + 보조 43) 적용, 배치표 미이행 0건. 전부 **서버 컴포넌트 정적 SVG**라 **First Load JS 102kB 무증가**·모듈 라우트 1.72kB/125kB 무증가 — 12종 × 140 인스턴스를 넣고도 번들 회귀 0. **8종 → 12종 확대(V-1)**: Step 0 전수 배치에서 8종으로 주 도해가 성립하지 않는 **22모듈**이 드러남(분류 트리 7·특성 곡선 5·기능 블록도 5·원자 격자 4·연표 4) → `CircuitSchematic`을 **`NodeGraph`로 일반화**해 회로도+블록도를 흡수하고 `TreeBranch`·`CurvePlot`·`LatticeDiagram`·`Timeline` 추가. `design-validator`가 **4건 적발**(V-2): 파일럿 019·029가 **2차 예정인 `NodeGraph`를 요구**하는 실행 불가 순서, 최다 사용 2위 `CompareCards`가 파일럿에서 미검증, §1.2 `col-3` 누락으로 커버율이 실제 96/97, §1.3 집계가 6개 항목 오류(주 도해 합 100 — 산술상 불가) → `NodeGraph` 1차 승격·파일럿 5모듈 확대로 해소. **`idPrefix` 규약**: 서버 컴포넌트라 `useId()`를 못 써 전역 `id`를 만드는 2종(`LayerStack`의 `<pattern>`·`NodeGraph`의 `<marker>`)만 필수로 좁힘(V-3) — 한 페이지 2도해(019·058)에서 충돌 0 실증. **원문 근거 규약이 9건을 되돌림**: col-8 욕조곡선 부재(배정 자체 교체·V-4), 001 저항률 상한 `1e14`, 087 High-k 유전율(원서는 재료명만), col-2·col-3 연도, 078 웨이퍼 지름 세대, 047 V_TH 정성값, 050 노드 이력, col-7 클래스 등급 — 특히 001·087은 **다른 표에서 값을 끌어오거나 상식으로 채운** 경우로, 도해 작업 특유의 함정임이 드러나 `usage.md`에 명문화. **자수 측정 로직 결함 발견(V-5)**: `<[^>]+>` 정규식이 도해 JSX 블록을 통째로 삼키는데 내부에 `<strong>`이 있으면 거기서 끊겨, 같은 작업인데 011은 ±0·018은 +141로 잡혔다 → 줄 단위 제거로 교정하고 선행 사이클 §5.1에 예외 문안까지 역반영. Check 1차 **78.1%**(Gap High 4·Med 3·Low 4) → 사용자 결정 "보조를 채운다"로 26건 전량 적용 + 배치표 12건 중 7건 이행·5건 정정(V-6) → **93.6%**(`pdca-iterator` 미사용, 수동 Act). **선행 사이클 문서 2건 갱신**: §5.2를 **T1 정밀 / T2 패턴 / T3 시각 강화 3단 판정**으로 재정의(V-7 — 초판의 "기존 컴포넌트로 표현 가능" 조건은 인프라 0이던 시절의 억제책이었다), §5.1 분량 기준 현행화(항목 1,200~2,300 / COLUMN 600~1,600) + 도해 자수 예외. **PR [#29](https://github.com/DrunkenZealnut/semiconductor-academy/pull/29)** · 커밋 5(feat `8db9620` 127파일 → chore `acf73dd` → merge `16e352f` main 병합 충돌 해소 → docs `afd316d` → docs `b928709` 보고서+리뷰 반영) · merge commit `3bb46b5` · +5,709/−342. **main 병합 충돌**: 같은 시기 PR #28(cert-equip-maintenance)이 머지돼 `pdca-status.json` 충돌 → 한쪽을 버리지 않고 features·history(450건)·phaseHistory를 수동 병합. CodeRabbit **1차 3건 전량 반영**: 상태 파일 보조 수량 10/36→43/43, `commit` 필드 해시-only 분리, **CLAUDE.md 배포 계약 정정**(GitHub Pages·`output:'export'` → Vercel + 미들웨어가 정적 export를 막는 이유 명기 — Do 중 이미 발견해 Plan에만 기록했던 것을 본문까지 수정). ⚠️ **미완: 게이트 G-1(라이트·다크·375px 실렌더)·G-3(시각 언어 대조) 육안 미확인** — 로그인 게이트 + Chrome 확장 미연결로 자동 확인 불가, 정적 대체 검증(하드코딩 색 0·본문 가로 스크롤 경로 없음·SVG id 충돌 0)만 수행. Claude 초벌(도해 기술 정확성 사람 검수 권장). Branch `feat/diagram-component-set` → `main` |
| [diagram-set-finishing](./diagram-set-finishing/) | 2026-08-11 | 97.3% | 0 | 1일(1세션) | **도해 12종의 미완 6건 마감 — 확대 전 계약 고정**. 선행 `diagram-component-set`(93.6%)이 남긴 백로그 6건을 닫았고, 그중 둘은 부채가 아니라 **계약**이었다 — 글자 크기 단위와 `altTable` 방침은 도해가 붙은 **모든 모듈**에 영향을 주므로 확대 후에 바꾸면 손봐야 할 모듈이 97에서 **310**으로 늘어난다(미적용 213모듈 = ncs-semi 84 + hs-\* 8종 96 + cert-equip 13 + daegu·osha 20, 책 17장 별도). **Plan이 백로그 판정 2건을 실측으로 뒤집었다**: F-1은 "미구현"이 아니라 **부정확 교정**(007이 전도띠에 `silicon-n`, 금지대역에 `oxide` tone을 빌려 쓰고 채움 패턴 `+`/`−`까지 나와 **도핑 도해로 오독될 여지**), F-2는 구현이 아니라 **삭제**(`Timeline` 사용처 2곳이 전부 이벤트 목록만 쓰고 세로 목록 렌더에는 시간 축 자체가 없어 `span`이 현행 모델과 불일치 — YAGNI). **계약 2건 확정(사용자 결정)**: **C-1 글자 크기 viewBox 절대 단위** — SVG 내부에서 `em`은 부모의 CSS px를, viewBox는 사용자 좌표계를 쓰므로 섞으면 375px에서 라벨이 도형을 넘친다, 접근성은 브라우저 확대(벡터)와 표 병치로 확보 · **C-2 표 병치를 대안 이행으로 명문화** — 89모듈이 이미 표를 지우지 않고 병치해 정보 손실 0인데 131개에 `altTable`을 채우고 표를 지우면 자수가 줄어 하한 근접 6모듈이 §5.1 보정에 걸린다, **이미 작동하는 방식을 규칙으로 승격하는 편이 정직하다**. 둘 다 **코드 변경 0** — 선행 사이클이 묵시적으로 택했으나 기록하지 않은 것이 Gap의 실체였고, 그래서 **이 사이클의 절반이 문서를 실제에 맞추는 일**이었다(코드 +130/−36 vs 문서 +492/−25). 구현: `LayerStack.orientation="band"`(기본값 `vertical`로 **기존 27개 사용처 무변경** — `rx=0` 각진 모서리·왼쪽 `↑ E` 에너지 축·금지대역은 채우지 않고 점선 테두리로 "존재할 수 없는 영역"을 빈 공간으로·채움 패턴 미적용) + **밴드 전용 tone 3종**(`band-conduction` teal-50/950 · `band-gap` transparent · `band-valence` indigo-50/950, **`mark` 없음**이 설계 의도 — p/n 도핑과 시각적으로 갈린다) + **SVG 6종 `<desc>` 데이터 자동 생성**(prop으로 받으면 131개에 일일이 써야 하고 누락이 생긴다 → **저작자 입력 0 · 누락 구조적 불가**). `aria-labelledby`는 6종 전부에 내부 id를 요구해 선행 V-3의 `idPrefix` 2종 축소와 충돌하므로 **`aria-label` 유지**(스크린리더가 우선 읽어 접근성 손실 0). 008의 도너·억셉터는 '띠'가 아니라 '준위'(선에 가까움)라 **`accent` 유지** — 띠 사이 디딤돌임을 강조. **Check가 마감 자체의 미완 7건을 다시 찾아냈다**(1차 84.0% → 즉시수정 → **97.3%**, Act 0·`pdca-iterator` 미사용): High 1 **G-7 선행 Design §2.2 props 표 7행이 구현과 불일치** — `CurvePlot.curves[].tone?`은 **시그니처만 있고 구현에 없는 prop**이라 G-7 조건("0건")을 정면으로 깸(표를 구현에 맞춰 정정, 구현이 나은 판단을 한 곳은 근거를 괄호에 — 코드 변경 0) · Med 4(**M-2 band+wells 조합 시 무효 `url(#)` 참조** — `marks` Set은 `!isBand`일 때만 채우는데 wells 패턴 rect에 가드가 없던 **잠복 버그**, 사용처 0이라 미발현이었으나 다음 저작자가 쓰면 바로 깨졌을 것 / **M-3 자수 예외 근거 문장의 쌍둥이 미정정** — `first-semiconductor` §5.1만 고치고 그 규칙의 출처 문단은 여전히 "altTable에 남으므로"였다, **정정 목록 자체가 항목을 빠뜨린 사고** / M-4 `_INDEX.md` 백로그에 이 사이클이 닫은 6건이 전부 미구현으로 남아 있음 / M-1 금지대역 점선이 §2.2의 "상·하 경계만"이 아니라 4변 전체 → 구현이 의도에 더 맞다고 판단해 **Design 문구를 정정**) · Low 2(L-1 `E` 글자 축선 겹침·L-5 `NODE_KIND` 배럴 재수출). **교훈 — 자기 검증 스크립트를 믿지 말 것**: Do ⑦에서 돌린 props 대조 스크립트가 "불일치 0"을 냈으나 `gap-detector`가 **7행 불일치**를 적발했다(optional prop만 정규식으로 훑고 제외목록 `at`·`to`·`label`이 과도해 대부분 건너뜀). 선행 `first-semiconductor-primer`도 자기 검증이 **4번** 틀렸고 그때 남긴 백로그가 하필 **"Gap 판정 스크립트에 양성 대조군"** — 이번에 왜 필요한지 다시 증명됐다(백로그 유지). Positive 5(문서정정 **8건**>목록 6건·`<desc>` 문안이 최소 규칙 초과·band 전용 접두어 "아래에서 위로 에너지가 높아진다"·`tokens.ts` 주석에 도입 근거·M-2 선차단). **Report 단계에서 미이행 DoD 1건 발견·수정**: "G-1·G-3 육안 확인 대상 목록 정리(사람 인계용)"가 Check까지 미이행 — M-4가 백로그 완료 표시만 갱신하고 육안 확인 줄은 손대지 않아 **렌더가 바뀐 유일한 2모듈 007·008이 인계 목록에서 빠져** 있었다(Match Rate 48항목에 이 DoD가 검증 항목으로 없던 것이 놓친 경로) → 007·008을 우선 항목으로 추가하고 확인 지점 4개 명시. 게이트 전부 통과 — Report 단계에서 **재실측**(기록 이관 아님): typecheck 0 · lint diagram 0(기존 `Lightbox.tsx` 경고만) · **First Load JS 102kB 무증가** · **SSG 393/393**(prerender HTML 389 — Next.js 카운트가 특수 페이지 포함) · `019` `rx="4"` 9건 유지(vertical 무회귀) · 007 패턴참조 **0건**·`rx="0"` 3 · 008 `rx="0"` 5 · 055·080 `<desc>` fallback 렌더 · cross-link 인덱스 내용 무변. **PR [#30](https://github.com/DrunkenZealnut/semiconductor-academy/pull/30)** · 커밋 4(feat `75c9ff6` → chore `dc5c189` → fix `f3fbb94` CodeRabbit 4건 → docs `3354505` 보고서+인계목록) · merge commit `57e50db` · +711/−68 · 20파일 · 체크 4/4 pass. CodeRabbit **4건 전량 반영**: Major 2(**Plan §1.1 표가 본문 213모듈을 재현하지 않음** — 표가 틀렸고 실측하니 `hs-*`는 8종 96, 소계 행+검산식 추가 / **Plan FR-7 `aria-labelledby` vs Design §5 `aria-label` — 하나의 계약에 두 기준**, FR-7을 정정하고 V-3 충돌 이유 기재해 다음 구현자가 올바른 구현을 실패로 판정하는 것을 막음), Minor 2(`LabeledFigure`에서 `desc`·`labels` 모두 없으면 `<desc>` 생략 → "6종 전부 설명 보유" **계약 위반을 타입상 허용되는 입력으로 짚어낸 값진 지적**, `caption`→`'도해'` fallback으로 무조건 렌더 / MD038은 **중첩 백틱 이스케이프**가 파싱을 깨뜨린 것이라 타입 표기를 `{at, text}[]`로 단순화). ⚠️ **미완: 007·008 육안 확인** — band 모드가 렌더를 바꾼 유일한 2모듈인데 로그인 게이트 뒤라 사람만 가능하다. **밴드 tone은 재료 tone(200/900번대)보다 연한 50/950번대라 12종 전체에서 다크 모드 대비가 가장 약한 조합** — 정적 검증으로 걸러지지 않는다. Claude 초벌(밴드 에너지 폭 비율은 원문 근거 없이 **시각적 균형으로 배분** — 사람 검수 권장). Branch `feat/diagram-set-finishing` → `main` |
| [diagram-expansion](./diagram-expansion/) | 2026-08-13 | 91.7% (4웨이브 90.9~92.3) | 2 | 3일 | **도해 12종을 시각 자료 0인 자료원 전량으로 확대 — umbrella 사이클 4웨이브 완주**. **156모듈 · 349 인스턴스**(주 156 · 보조 193), 본문 **+5,765 / −0**(한 줄도 지우지 않음 — C-2 표 병치). 전수 구간 **129/129**(W1 교과서 4권 45 + W2 교과서 잔여 4권·daegu 61 + W3 수험서·OSHA 23)로 **D-2 충족**, W4는 도판 보유 101모듈 중 **27 선별**. **Plan이 백로그 전제를 실측으로 정정**: "213모듈 확대"는 균일 대상이 아니었고 도판 보유 101모듈은 이미 원문 도판 425장을 갖고 있었다 → W4를 전수가 아닌 **중복 판정**으로 재정의(G-14 신설). W4 분포가 그 판정의 증거다 — `ValueBars` 9건인데 `LayerStack`·`NodeGraph` **0건**(도판이 있으면 구조 그림은 중복이고, 표에 흩어진 수치 비교만 도판이 못 한다). **검사기가 이 사이클의 본체 산출물**: C-1~C-9 → **C-1~C-17**, 대조군 12 → **25**(양성 17·음성 8, 자기검사 실패 시 본 검사 미실행 exit 2). 붙일 때마다 **통과 중이던 코드에서 결함이 나왔다** — W1 C-1~C-6 → 보조 미이행 7건 / W2 C-7 → 조용한 잘림 5건(`[:6]` 슬라이스가 표를 경고 없이 잘랐고 caption은 정답을 알고 있었다) / Act① C-1주제·C-8용어·C-9참조 → **창작 8건·계획 이탈 11건·Design 표 어긋남 19곳** / W3 C-10·C-13 → ko 라벨 en 유입·문단 쪼갬 / Act② C-11~C-17 → 계산값 주장 7건(**둘은 수치 자체가 틀렸다** — "열두 자릿수"의 실측은 14.9). **최대 발견은 `element-pneumatic-thermal`** — 본문에 중온도 PECVD도 0회인 막대를 C-2가 통과시켰다("400"이 다른 문맥에 있었다). **수치 하나하나가 본문에 있어도 짝지음이 창작이면 거짓이다.** 배치표를 Design §2에서 **생성**하게 만든 즉시 Design 표가 구현과 19곳 어긋난 것이 드러났고(사람은 산문을, 기계는 표를 읽는데 둘이 갈라져 있었다), 손으로 옮긴 집계가 **8번** 틀려 규약을 세웠다 — **문서에 적는 수치는 검사기 출력을 옮긴다**. 예외 키를 `모듈::검사::식별자` → **`자료원/모듈::검사::식별자`**로 전량 이관(동명 모듈 4쌍이 서로의 예외를 쓸 수 있었다 — CodeRabbit이 뒤에 `modShape`에서 **같은 버그**를 찾았다). ★**G-9 육안 확인 종결 — 3사이클 묵은 미완 게이트**. 헤드리스 Chrome으로 로그인→뷰포트→요소 캡처+계측을 스크립트화(확장 스크린샷이 첫 로드 후 계속 타임아웃). A~F 6군 확인 후 **결함 3건 적발·수정**: ①**375px에서 SVG 도해가 눌려 라벨이 안 읽힌다** — viewBox 640이 컨테이너 343px로 축소돼 ×0.54, 라벨 13px→**7.0px**·부라벨 11px→**5.9px**. `min-w-[18~20rem]`(288~320px)가 가용 폭보다 작아 최소 폭이 **발동조차 못 했고** `LayerStack`·`LatticeDiagram`은 `scrollable`도 안 넘겼다 → SVG 6종 전부 `style minWidth = viewBox 폭` + `scrollable`, **109 인스턴스 / 94 모듈**(사이트 전체 SVG 계열 전량) ②**`accent` 톤 대비 미달** — `fill-brand-500` 하나로 라이트·다크가 같아 **3.97 / 3.36**(AA 4.5) → `brand-300 dark:brand-900`으로 8.11 / 9.45. `brand-100` 안은 `indigo-50`과 구분이 안 돼 **note가 약속한 "강조색으로 구분했다"가 거짓이 되므로** 탈락 ③**`LatticeDiagram` 설명문이 아래쪽 원자와 겹침** + `max-w-md`(448) 탓에 **데스크톱에서도 7.7px** → viewBox를 350으로 크롭 + `LABEL_H` 확보. **교훈 — 정적 검사는 화면을 보지 않는다**: 세 결함 모두 MDX와 컴포넌트 코드가 둘 다 옳은데 브라우저에서 틀렸다(viewBox는 정확했고 "컨테이너 폭"이라는 제3의 변수를 아무도 안 봤다 / tone의 유효성 C-3과 tone·TEXT 조합의 대비는 다른 문제다 / 좌표는 코드가 계산해 창작이 아니지만 겹침은 배치 문제다). **검사 대상이 소스인 한 렌더 결과는 영구히 검사 밖에 있다.** `ValueBars` 단위가 `note`에 덮여 사라진 **22건**(기준선 8 + 이번 확대 14)이 그 표본 — `unit` prop이 MDX에 정확히 있었기에 4번의 Check와 3번의 gap-detector를 그대로 통과했다. 재검증: SVG 94페이지 × {375, 1440} **가로 넘침 0 · 실효 글자 9px 미만 0** · 대비 **AA 미달 0**(최저 5.79) · 검사기 4웨이브 전 항목 통과 · typecheck 0 · **102kB 무증가 · SSG 393/393**. **PR [#32](https://github.com/DrunkenZealnut/semiconductor-academy/pull/32)**(`7ce01ee` 검사기·컴포넌트 3파일) → **PR [#31](https://github.com/DrunkenZealnut/semiconductor-academy/pull/31)**(`2e87ef3`, 콘텐츠 `d711d1b` 179파일). **순서가 의도적이다** — `ValueBars` 단위 수정이 도해 22건의 표시에 필요한 기능 의존성이라 코드를 먼저 넣었다. `git rebase`는 **쓰지 않았다**: #31의 두 번째 커밋이 스크립트를 *삭제*하므로 새 main 위로 리베이스하면 그 삭제가 살아난다 → `reset --soft main` + `checkout main -- scripts` 로 179파일 단일 커밋 재구성. CodeRabbit **4라운드 9건 전량 반영**(활성 버그 4 · **내가 만든 회귀 1** — 미종료 블록이 다음 블록의 `/>`를 먹으면서 경고도 안 냈다, C-17로 고정 · 프로토타입 체인 1 — `WAVES[k]`가 `constructor`·`toString`·`__proto__`를 받았다 · 반증 1 — C-13 예시는 재현되지 않았으나 **근본 문제는 실재했고 방향이 더 나빴다**(거짓 통과)). **에이전트 주장 반증 2회**(gap-detector의 "`probe-test`에 '세 축'이 없다"는 §2 제목이 정확히 그것이었다). 아카이브 시 **살아 있는 자산은 원위치 유지**(`diagram-expansion.w1~w4-plan.json` 검사기 `--plan` 입력 · `diagram-check-allow.json` 예외 43건 · `diagram-component-set.usage.md`), `scripts/build-diagram-plan.mjs`의 Design 경로 4개는 **아카이브로 갱신**하고 배치표를 재생성해 `--check` 4웨이브 일치를 재확인했다 — Design §2 표는 동결된 기록이어야 하고 배치표는 그 기록과 계속 일치해야 한다. ⚠️ **G-9 수정분(`src/components/diagram/` 7파일 +47/−11) 미커밋** — 커밋은 사용자 요청 시에만(프로젝트 규칙). 잔여: `NodeGraph` 간선 라벨 9px(키우면 겹침 위험) · 렌더 계측의 상시 관문화 판단 · `altTable` 9/109. Claude 초벌(도해 기술 정확성 사람 검수 권장). Branch `feat/diagram-expansion`·`feat/diagram-verification` → `main` |
| [diagram-render-gate](./diagram-render-gate/) | 2026-08-13 | 100% | 0 | 1일(1세션) | **G-9가 손으로 잡은 렌더 결함군을 상시 검사로 고정**. 선행 `diagram-expansion` §4.6이 "검사 대상이 소스인 한 렌더 결과는 영구히 검사 밖에 있다"로 끝났고, 그때 만든 계측기는 **세션 스크래치패드에만** 있었다. **Plan이 백로그 전제를 정정했다** — "렌더 계측을 상시 관문으로"는 브라우저 상시 실행으로 읽히지만 **셋 중 둘은 브라우저가 필요 없다**: SVG 축소는 코드 계약, 대비는 `node_modules/tailwindcss/theme.css`의 **oklch 288색** + `globals.css` brand hex + `tokens.ts`를 **읽어서** sRGB로 변환해 계산된다. 타당성을 **G-9 브라우저 실측 12쌍을 정답지로 증명**했다 — 최대 오차 **0.060**이고 통과분만이 아니라 **미달 3건(3.36·3.97·4.01)까지 재현**해 AA 판정이 브라우저와 동일하다. 그 12쌍을 음성 대조군으로 **고정**했다: 계산식을 손대면 본 검사보다 자체검사가 먼저 깨진다. **분업 경계** — 정적 관문은 **토큰이 결정하는 것**(밀리초·의존성 0), 브라우저는 **좌표가 결정하는 것**(약 3분). 중복이 아니다. **C-18**은 식 비교를 버리고 `svgBox()` 헬퍼 사용 검사로 했다 — `viewBox`와 `minWidth`가 **한 인자에서** 나와 어긋난 상태를 만들 수 없다(`diagram-set-finishing`이 `<desc>`를 자동 생성으로 정한 것과 같은 논리: 저작자 입력 0·누락 구조적 불가). 다섯 조건 전부 **문자열 존재/부재**라 식을 해석하지 않는다. 부수 효과로 CodeRabbit이 PR #33에서 지적한 유한·양수 가드가 한 곳으로 모였다. **C-19**는 조합을 **발생 기반이 아니라 가능성 기반**으로 골랐다 — `badge`가 `io` 노드에 쓰이는 순간 깨지는데 발생 기반이면 그 조합이 검사에서 빠지고, **컴포넌트를 한 줄도 안 고쳐도 콘텐츠만으로 깨진다**. ★**관문이 AA 미달 5건을 실제로 잡았다**: `TONE.metal` **4.40**(zinc-500, 사용 36건 → zinc-600 7.05. 휘도 0.086으로 다른 다크 tone 0.017~0.052보다 여전히 밝아 금속성 유지, zinc-700은 slate-700과 섞인다) · `NODE_KIND.io` **3.84**(badge를 `TEXT`로 → 9.19, 9px 글자를 흐리게까지 하지 않는다) · `silicon-p-heavy` **3.94**/**3.01** · `silicon-n-heavy` **2.86**. **뒤 셋이 이 사이클의 핵심 사건이다** — `<pattern>` 안 마크 글리프(`+`/`−`)이고 C-19가 처음에 **보지 못했다**. `TONE × TEXT_MUTED` 26조합을 뺀 근거가 "`TEXT_MUTED`는 전부 층 밖"이었는데 **`x={4} y={12}`를 좌상단 축 라벨로 잘못 읽은 것**이었다 — 그것은 16×16 패턴 타일 좌표이고 패턴은 층 rect 위에 같은 기하로 덮인다. 그 글자는 장식이 아니라 `usage.md` §3 "색만으로 구분하지 않는다"를 이행하는 **색각 이상 대비 장치**라 읽히지 않으면 색을 구별하지 못하는 독자에게 p형·n형이 같은 층이 된다. **8건 5모듈에서 실제로 렌더되고 있었다.** 조치 순서를 지켰다 — ①조합을 임시로 넓혀(`--widen-tone-muted`) 검사가 3건을 잡는지 **먼저 확인**(마크 없는 tone 오탐 7건도 함께 확인해 제외의 필요성 재확인) ②글리프를 `TEXT`로 ③실물 확인 ④**손으로 읽어 틀린 판단을 기계 규칙으로**(`checkPatternTextClass()`). 개수만 보는 D-12 스냅샷은 **침묵했다**(3 그대로) — Design §7이 적은 "같은 개수로 위치만 옮기면 못 잡는다"와 다르고 **처음부터 위치를 잘못 읽은 경우**다. **자체검사가 제 일을 한 장면 하나** — 조합에서 ③(`NODE_KIND × TEXT_MUTED`)을 빼자 양성 대조군 "`TEXT_MUTED`를 `slate-500`으로 되돌림"이 **아무것도 검출하지 못했다**(`slate-500`은 남은 프레임 조합에서 4.76 통과). **본 검사는 그대로 통과하는데 자체검사가 먼저 실패했다.** 교훈: 범위를 **넓힐** 때 대조군을 더하는 것은 자연스럽지만 **줄일** 때 기존 대조군이 무효해지는 것은 눈에 띄지 않는다. **브라우저 심층 검증**(`verify-diagram-render.mjs` 265줄): 실효 글자 크기·페이지 가로 넘침·**좌표 기반 대비**·마커 참조. 좌표 기반 대비가 존재 이유다 — 정적 과대근사로는 `LabeledFigure`에서 **최대 64건**이 나올 자리인데 실제 좌표로는 **0건**이다(D-10 실증). `playwright-core` devDep + `channel:chrome`(브라우저 바이너리 미다운로드), 서버를 띄우지 않고 없으면 안내 후 `exit 2`, 자격 증명은 `process.env`만. ★`figures === 0` → `exit 2` — **아무 도해도 못 봤는데 "전 항목 통과"가 나오는 경로**가 있었다(미들웨어가 `/login`으로 **리다이렉트**하고 `page.goto`가 따라가 200을 주므로 `status >= 400` 망에 안 걸리고 `/login`에는 `figure svg`가 없어 findings가 0이 된다). gap-detector가 M-2로 확인. **npm 관문 2종**: `verify:diagram`(`--all` 신설, 4웨이브를 각자의 배치표와) · `verify:render`. `--all`은 편의가 아니다 — 아카이브 작업 중 `--plan`을 빼고 돌려 **W4가 전수 판정으로 오판**된 일이 있었고 인자를 손으로 넘기는 구조가 원인이었다. 검증: 자체검사 대조군 **25 → 38** · `verify:diagram` 4웨이브 전 항목 통과(C-18 SVG 6종 · C-19 조합 36건) · `verify:render` **92페이지 × {375, 1440}** 글자 있는 SVG **107개** 전 항목 통과 · typecheck 0 · lint diagram 0 · build exit 0 · **102 kB 무증가** · **SSG 393/393**. **Check**: gap-detector **18건**(High 2·Med 6·Low 10) 전건 처리 — 수용 17 · **프레임 반박 1**(H-2 "Design은 36이라 적지 않았다"는 틀렸다: §2.2·`usage.md`·실행 출력이 36이었고 §1 D-9 표·산술만 42였다. **"구현이 Design과 다르다"가 아니라 "Design 내부가 갈렸다"**가 정확하다). H-1은 구조 3건·수치·사용량을 **직접 재확인한 뒤** 수용. **G-5 미러 위반 2건은 내가 먼저 찾아 고쳤다** — 브라우저 스크립트의 SVG 6종 하드코딩 · C-18의 마커 `viewBox` 리터럴 지우기(마커 좌표가 바뀌면 오탐하는 새 미러였다). 고치다 **Design에 없던 조건**을 발견해 추가: `style=` 중복 전달 — JSX는 뒤에 오는 명시 속성이 이기므로 `style={{ maxWidth }}`를 뒤에 주면 `minWidth`가 **통째로 사라진다**(4조건 → 5조건). **gap-detector 첫 실행은 실패했다**(600초 무진행, 마지막 출력 "The script file appears to have changed since my first read") — **에이전트가 분석 중인 파일을 내가 동시에 편집했다**. 도구가 경고한 그것이고, 재실행 시 기존 발견 4건을 브리핑에 넣어 중복을 배제하고 내가 확인하지 않은 각도 4건을 지정했다. **손으로 센 수치가 세 번 틀렸다** — Plan `badge` 0건(실제 14, prop이 아니라 라벨 접두어라 MDX에서 `badge:`로 셀 수 없었다) · D-7 94페이지(실제 92) · D-12 `TEXT_MUTED` 계 12(실측 11). 세 번째는 **그 규약을 인용하는 문서를 쓰면서** 틀렸다. 주목할 점: 스냅샷 값 자체는 코드에 11로 맞게 들어가 검사가 통과했다 — **틀린 것은 문서뿐이다. 코드는 검사가 지키고 문서는 아무도 지키지 않는다.** 변경 수정 15파일 **+732 / −40** · 신규 4파일 **966줄**. Act **0회**(Check 안에서 즉시 수정으로 종결). ~~⚠️ **잔존 R-1이 가장 무겁다**~~ → ✅ **2026-08-16 `diagram-gate-coverage`가 해소** — SVG 92페이지 중 **60페이지가 C-1~C-17 범위 밖**이었다(`first-semiconductor`가 `WAVES`에 없다). `CLAUDE.md`에 **이 사이클이 적은** "도해를 추가·수정하면 `verify:diagram`"이 그 60모듈에는 **지켜지지 않는 약속**이다. Claude 초벌(대비·크기 판정은 실측값이나 시각적 적절성은 사람 검수 권장). Branch `feat/diagram-render-gate` → `main` |
| [diagram-gate-coverage](./diagram-gate-coverage/) | 2026-08-16 | 98% | 0 | 1일(1세션) | **선행 `diagram-render-gate`의 잔존 R-1을 닫았다** — 그 사이클은 "SVG 92페이지 중 **60페이지가 C-1~C-17 밖**"이라는 사실을 `CLAUDE.md`에 **주의로 적는 데서 멈췄다**. 약속을 좁힌 것이지 범위를 넓힌 것이 아니었다. 이 사이클이 `WAVES`에 `w0: ['first-semiconductor']`(97모듈 · **도해 140건 = 전체의 28.6%**)를 등록해 관문을 **4웨이브 349건 → 5웨이브 489건**으로 넓혔다. ★**41건 중 9건이 검사기 오탐이었다** — 4웨이브가 전 항목 통과인 상태에서 다섯 번째를 넣자 기존 검사기의 결함군이 처음 드러났다: C-8 8건(`refs` 미제외 4 · **`자`로 끝나는 낱말을 활용 서술어로 오인** 2 — 이 주제에서 `자` 끝은 거의 전부 명사다(전자·소자·원자·운반자, 라벨 실측 23건) · **`\n`을 가름 문자로 안 봄** 2 — 렌더러 `splitLabel`은 쪼개는데 검사기는 한 낱말로 봤다) · C-7 1건(`enumeratedCounts`가 `rows`를 후보 키로 안 봄). C-4 2건도 오탐이었다 — `split()`이 블록 끝을 여는 태그로 잡아 자식 SVG 첫 줄이 "뒤 빈 줄 없음"으로 보였다(`end: k`를 `end: j`로 되돌리면 2건이 그대로 재현된다). **자료원을 늘리는 일은 콘텐츠 검사이면서 검사기 검사다.** 처분: 검사기 수정·제외로 **22건 소멸** → 남은 19건을 **예외 16 · 고침 3**(`전극층`→`전극` · `기판`→`웨이퍼` · `sub` 1건). C-7 4건은 **전부 오탐**이라 고치지 않았다 — `044-sram`의 "트랜지스터 여섯 개"는 육안 확인 결과 **맞는 수**였다(노드 7개 중 소자 6 + 블록 1). **D-9가 이 사이클의 구조다** — `split()`이 자식 형태를 인식하고 자식 영역을 **"블록도 본문도 아닌 제3의 범주"** 로 둔다. 블록에 넣으면 `M 96 186 L …`이 전부 "본문에 없는 수치"로 쏟아지고, 본문에 두면 **더 나쁘다**: SVG 좌표가 본문 낱말이 되어 **다른 도해의 창작 값이 "본문에 있으니 통과"** 가 된다(등록 전 `first-semiconductor`가 그 상태였다). **D-10·D-11은 경고가 아니라 `exit 2`** 다 — 미등록 자료원·`idPrefix` 전역 중복(실측 85개 중복 0)은 콘텐츠 위반이 아니라 **검사기가 자기 범위를 주장할 수 없는 상태**라, 팔레트를 못 읽을 때와 같은 처분을 준다. ★**대조군이 통과하면서 공허한 세 번째 방식을 찾았다** — G-4 ②·⑤가 `C-2`만 보느라 D-1(C-6)·D-4′(C-8)의 **절반씩을 증명하지 못한 채** ✅를 내고 있었다(앞의 둘은 선행 사이클: 범위를 줄여 무효화, 건강한 검사가 0건을 내서 무효화). `silent` 목록으로 걸치는 검사를 다 보게 했고, 두 절반을 **서로 다른 장치**가 지킨다는 것도 드러났다 — C-2는 `childRegions`가, C-6은 "블록 텍스트는 여는 태그까지"라는 정의가. **검사를 느슨하게 바꿀 때는 음성 대조군이 필요하다** — C-8 2건·C-7 1건이 완화였다. 넓힐 때 양성을 더하는 것은 자연스럽지만, 느슨하게 할 때 "무엇을 **이제 통과시키는가**"를 고정하지 않으면 완화가 어디까지 갔는지 아무도 모른다. 루프에 `expectClean`을 추가했다. 대조군 **41 → 56**, 되돌려 실패하는 것까지 확인. ★★**G-9가 자동 검사 전 항목 통과 상태에서 렌더 결함 4건을 잡았다** — `LayerStack` 층 라벨이 `side:'left'` well에 덮여 `060-feol-1` "표면"은 **완전히 사라졌고** `019-cmos`·`021-mesfet`은 아래 획만, `068-cmp`는 절반이 잘렸다. 넷 다 새로 들어온 w0이다. **글자가 도형에 가려지는 것은 색 대비로도 폭 넘침으로도 나타나지 않는다** — `verify:render`가 못 보는 자리이고, G-9가 자동화로 대체되지 않는 이유의 실증이다. 높이를 키워 피하지 않았다 — `vertical`에서 **층 두께는 막 두께**라 얇은 표면층을 두껍게 그리면 도해가 거짓말을 한다. 라벨이 자리를 양보한다(빈 자리 계산). **그 수정에서 내 판정이 두 번 틀렸고 둘 다 실물이 잡았다**: ⓐ세로 겹침만 봐서 `065-etching`을 오탐으로 올렸다(라벨 왼쪽·well 가운데라 가로로 안 겹친다) → 6건→4건 ⓑ"가장 넓은 빈 자리"로 배치했더니 멀쩡하던 `065-etching` 라벨이 오른쪽으로 끌려갔다 → **"왼쪽부터 처음 들어가는 자리"** 로 바꿔 원위치. 겹치지 않는 라벨은 움직이지 않아야 한다. **이 4건은 범위 위반이다** — Plan §6·Design §6이 명시적으로 범위 밖에 둔 항목이라 D-13으로 **사후 기록**했고, 기록으로 해소되지 않으므로 Match Rate의 범위 준수 축을 **80%로 남겼다**. 전용 검사도 대조군도 없고 근거는 렌더 좌표 실측뿐이다(145.36 · 296.68 · 190.08 · 190.08, 회귀 확인 `065` 26). **Check(gap-detector 16건)에서 문서 결함 7건을 고쳤다** — 가장 무거운 것은 `usage.md` §3.1a가 코드와 **반대**로 적혀 있던 것으로, 오탈자가 아니라 **틀린 계약**이었다(문서대로 구현하면 `065-etching`·`061-feol-2` 2건이 회귀한다). 예외 4건의 이유도 틀렸다 — "축 눈금 이름"이라 적었으나 실제는 `curves[].label`(곡선 범례)이고 축 이름은 `axes`에 따로 있다. **예외의 성립은 맞고 근거가 틀린** 경우다. ★**손계산이 이 사이클에서만 네 번 틀렸다** — Plan §1.1 "등록 253모듈"(실제 156, 253은 156+97이라 미등록 비중을 27.7%로 보이게 했다. 실제 **38.3%** — 자기 근거를 **약하게** 적은 오류) · Design 대조군 산식 53(D-10이 양성·음성 둘) · Plan D-3 viewBox 2건(실제 3 — `055-cz-growth`의 `640`도 viewBox였다) · 보고서 "41→0" 분해 9+16+3=28. **실측표를 고정해 넘겨도 파생 계산을 하는 자리에서 또 틀렸다.** 검증: `verify:diagram` **5웨이브 전 항목 통과**(대조군 56/56) · typecheck 0 · lint diagram 0 · build **SSG 393/393** · First Load JS **102 kB 무증가** · `verify:render` 92페이지×2뷰포트 · 글자 있는 SVG 107개 전 통과. 변경 8파일 **+435 / −37**. Act **0회**(Check 안에서 즉시 수정으로 종결). ⚠️ **잔존 L-3이 가장 무겁다** — D-10·D-11 대조군이 `hard: false`라 **`exit 2` 처분 자체는 미검증**이다. "경고가 아니라 실패"가 D-10의 핵심인데 그 핵심이 대조군 밖에 있다. Claude 초벌(좌표·대비는 실측값이나 시각적 적절성은 사람 검수 권장). |
| [diagram-gate-exit-contract](./diagram-gate-exit-contract/) | 2026-08-24 | 97% | 0 | 1일(1세션) | **선행 `diagram-gate-coverage`의 잔존 L-3을 닫았다** — D-10이 *"경고가 아니라 `exit 2`"* 라고 **결정문에 적어 놓고**, 대조군은 `hard: false`로만 돌아 **"감지한다"까지만** 증명했다. **"감지하면 실패한다"** 는 결정문 자체가 검사 밖이었다. ★**R-1과 같은 모양이다** — 사실을 적는 데서 멈춘 항목. 착수 전 실측: `exit 2` 경로 **12개 중 종료 코드로 검증된 것 0개**(대조군 56건은 전부 in-process 반환값만 본다). 현재 **19개**. **이음매는 cwd다** — 검사기가 전부 cwd 상대 경로로 읽으므로 임시 디렉터리에 `node_modules`·`docs`·`src/components`·`src/styles`를 심링크하고 `src/content`만 진짜로 만들면 된다. **생산 코드에 검사 전용 이음매 0.** 환경변수로 root를 재정의하는 안은 버렸다 — **실제 실행에서 잘못 설정되면 관문이 엉뚱한 트리를 보고 눈먼 채 통과한다.** 검사를 강화하려다 이 저장소가 반복해서 막아 온 구멍을 새로 뚫는 것은 앞뒤가 안 맞는다. ★**재귀가 이 사이클의 그림자다** — 시작 단언은 `selfTest` 앞에서 돌고 대조군은 `selfTest` 안에 산다. 자식을 `--self-test`로 띄우면 자식의 `selfTest`가 또 자식을 낳는다. 양성은 **우연히** 안전하고(단언이 먼저 터진다) 위험한 것은 **음성** 대조군이다 — *"정상일 때 아무 일도 안 일어난다"* 를 확인하려다 무한히 프로세스를 만든다. `--assert-only`로 끊었다(환경변수 가드가 아니라 인자로 한 이유: 숨은 검사 전용 스위치가 아니라 **사람도 쓸 수 있는 정직한 의미**를 갖는다). ★★**그런데 Check에서 다시 만들었다** — ⑧(`--all` 배관)의 초안이 자식을 `--all`로 띄워 본 검사까지 가게 했고, **관문이 통째로 깨진 뒤에야** 알았다. 웨이브 자식이 **시작 단언에서 죽는** 시나리오로 바꿔 풀었다. 교훈: **재귀 회피는 인자에 붙는 성질이지 코드베이스에 붙는 성질이 아니다.** **Check(gap-detector 16건) High 3건이 전부 실재했다.** ★**H-1이 가장 아프다 — 이 사이클이 세운 규칙을 이 사이클의 대조군이 어겼다.** Design D-3이 *"종료 코드만 보면 공허하다, 사유 문자열까지 본다"* 를 정했는데, ④의 사유 정규식 `/tokens\.ts/`가 **성공 배너**(`tokens.ts Tone 13종 로드`)에 걸려 **D-10 때문에 죽은 실행도 만족**했다(실측: `/tokens\.ts/` true · `/tokens\.ts를 읽을 수 없다/` false). **규칙을 만든 자리에서 그 규칙을 어기기가 이렇게 쉽다.** ★**H-2 — 고치지 않기로 한 것에 절대 명제를 썼다.** Do에서 `globals.css`에 가드가 없는 것을 발견하고 "범위 밖이니 Check에 올린다"고 판단한 것은 **옳았다**(선행 사이클의 감점 사유를 반복하지 않았다). 틀린 것은 **그 상태로 `CLAUDE.md`·`usage.md`에 "팔레트·토큰을 못 읽으면 2"를 단서 없이 못 박은 것**이다 — 세 경로(`globals.css` 부재 · 콘텐츠 루트 부재 · Tone 파싱 실패)가 전부 **1**을 내고 있었다. **고치거나 단서를 달거나 둘 중 하나였어야 했다.** Check에서 고치는 쪽을 골랐다 — **Check가 찾은 것을 고치는 것은 범위 위반이 아니라 사이클이 도는 것이다.** **H-3 — D-10의 거울상이 뚫려 있었다.** 미등록 디렉터리에 도해가 있으면 `exit 2`인데, **등록됐는데 디렉터리가 없으면** 경고 한 줄 뒤 **`✅ 전 항목 통과`** 였다(w1 자료원 넷을 다 지워도 통과). **D-14 시작 단언**을 신설했다 — 웨이브 루프가 아니라 시작 단언에 둬야 `--assert-only`로 검증된다. **처분 되돌림 6종 전부 비공허 확인**(D-10 · D-11 · `tokens.ts` · `globals.css` · 콘텐츠 루트 · D-14). 각각 **해당 대조군만** 깨진다. ★그중 `globals.css`에서 **되돌림이 불완전해 "되돌렸는데 통과"가 나왔다** — 가드가 두 곳인데 하나만 되돌려 다른 하나가 먼저 걸렸다. **대조군이 공허한 것이 아니라 실험이 틀린 것**이었다. *"되돌렸는데 통과"를 곧바로 "공허하다"로 읽으면 멀쩡한 대조군을 버린다.* **새 단언은 기존 대조군의 전제를 바꾼다** — D-14를 넣자 최소 트리를 쓰던 ①③이 깨졌다(등록 자료원 14개 중 하나만 만들었기 때문). `scaffold` 옵션으로 등록 디렉터리를 전부 빈 채 만들게 고쳤다. ⚠️ **G-7은 미충족으로 남겼다** — `--all` **0.56 → 1.72초**. 대조군 8건이 웨이브마다 돌아 **손자 40개**가 뜬다. 중복 제거에는 *"자식이 자기가 자식임을 아는"* 이음매가 필요하고 어떤 형태든 **잘못 쓰면 대조군을 건너뛴다** — 이음매 설계 문제이지 대조군을 더해 풀 문제가 아니다. 대신 비용을 인정하고 `CLAUDE.md`를 실측값("약 1.7초")으로 고쳤다. ★**"빠르다"를 "문서와 맞다"로 재정의하면 G-7을 통과시킬 수 있었지만 그러지 않았다 — 게이트를 통과시키려고 게이트의 뜻을 바꾸는 일이다.** 검증: `verify:diagram` 5웨이브 전 항목 통과(**대조군 56 → 64** · 종료 코드 대조군 **8건**) · typecheck 0 · lint diagram 0 · `src/` 오염 0 · TMPDIR 잔존 0. 변경 3파일 **+293 / −11**. Act **0회**(Check 안에서 즉시 수정으로 종결). **종료 코드가 이제 공표된 계약이다** — `0` 통과 · `1` **콘텐츠 위반** · `2` **검사기가 자기 범위를 주장할 수 없음**(`CLAUDE.md`·`usage.md` §2.1.1f). Claude 초벌. |
| [diagram-label-legibility](./diagram-label-legibility/) | 2026-08-27 | 94% | 0 | 3일 | **선행 `diagram-render-gate`의 잔존 R-3을 닫았다** — `+`/`−` 채움 글리프가 층 라벨과 **같은 `TEXT` 색·비슷한 굵기**라 letterform에 섞여 `012-pn-diode`의 **"애노드"가 "애노두"** 로 읽혔다. ★**대비가 아니라 배치 문제**라 C-19도 `verify:render`도 못 잡았다 — 위험군 **53개 영역 · 27모듈**(층 44 · wells 9). **글리프를 지우는 것은 답이 아니다** — 색각 이상 대비 장치이고(usage.md §3) 선행 사이클이 이 글리프의 대비를 AA로 올리느라 한 항목을 썼다. 지우면 p형·n형을 색으로만 구분하게 된다. **라벨 뒤 판은 높이 분포로 죽었다** — 최박 층이 **h=16**인데 판은 패딩 포함 17~19px이라 **판이 층보다 크다**(h≤30이 12개). 대신 **패턴에 마스크로 구멍**을 낸다: 층 색이 남고 글리프는 라벨 밖에서 계속 보인다. 구멍 네 변은 **타일 격자(16)에 바깥쪽 스냅** — 안 하면 경계가 타일 중간을 지나 글리프가 반만 남고 라벨 옆에 점 같은 잔여물이 생긴다(실측). **Plan FR-4를 정정했다** — "D-13도 같은 규칙에 담는다"는 **하나로 담을 수 없다**: R-3은 글리프가 라벨보다 **먼저** 그려져 섞이는 것이고 D-13은 wells가 **나중에** 그려져 덮는 것이다. 판정 방법도 다르다. ★★**분업이 실증됐다** — 구멍 좌표를 0으로 만드는 **한 줄**이 층 44건을 통째로 되돌리는데 정적 `C-20`은 **통과**하고 브라우저 규칙이 **88건**으로 잡는다. 둘 중 하나만으로는 이 결함군을 못 지킨다. ★**Check가 찾은 가장 큰 것 — D-4 후단(브라우저 규칙)이 아예 없었다.** Do가 G-6을 *"`verify:render` 전 항목 통과"* 로 닫았는데 **참이면서 약속한 새 항목을 안 더한 것을 가렸다.** Plan §1.2가 이 사이클을 시작한 이유가 *"`verify:render`는 대비와 넘침만 본다"* 였는데 Do가 끝난 시점에도 그대로였다. **게이트 문구가 참이면서 사실을 가릴 수 있다 — "전 항목 통과"는 항목 집합이 고정일 때만 뜻이 있다.** ★**브라우저 규칙을 다섯 번 고쳤다**: ①`best`(최소 포함 사각형) → **공허**(바탕 rect와 패턴 rect가 **면적 동률**이고 엄격 비교라 바탕이 이겨 규칙을 통째로 건너뜀) ②포함 패턴 rect 전부 → 오탐 ③맨 위 **패턴** → 오탐(그 위의 **불투명** `metal` well을 못 봄, `020-jfet` 육안) ④맨 위 **도형** → 오탐(여유 **−0.1유닛**, 브라우저 좌표 실측) ⑤여유 2유닛 → 통과. **다섯 번 모두 추론이 아니라 측정으로 알았고 3·4차는 "이제 맞다"고 판단한 뒤였다.** 여유 2유닛은 임의값이 아니라 글리프 기하에서 나왔다(구멍 경계가 타일 경계이고 다음 타일 글리프는 약 3유닛 떨어져 시작). ★**성능 진단이 틀렸다** — Do 중 `verify:render`가 10분을 넘겨 "3분 → 10분"이라 판단하고 두 순회를 합쳤는데, 재 보니 **106초**로 기준선보다 빠르다. 원인은 규칙이 아니라 **죽었거나 깨진 빌드를 서빙하던 서버**였다(`waitUntil: networkidle`이 매달림). 합친 최적화는 유지하되 **그 이유로 한 것이 아니었다**를 남겼다. **부수 수정 넷** — ⓐ`verify:render`가 `defs`·`mask` 안의 도형을 배경으로 읽어 멀쩡한 라벨 **106건**이 대비 1.44로 잡혔다(**원래 있던 결함**, `pattern` 안에 `rect`가 없어 안 드러났을 뿐. 마스크를 defs로 옮기는 것으로는 **안 고쳐진다** — `querySelectorAll`은 defs도 훑는다) ⓑ**주석에 쓴 태그 이름이 C-19를 깨뜨렸다**(`indexOf` 스캐너가 주석을 여는 태그로 오인해 뒤의 `TEXT_MUTED`까지 삼켰다 — 문자열로 훑는 검사가 있는 파일에서는 주석도 코드처럼 읽힌다) ⓒ종료 코드 대조군이 `process.argv[1]`을 **상대 경로**로 자식에 넘기고 있었다(선행 사이클 **잠복 결함**, 자식은 cwd를 바꾼다) ⓓ★**§7.1이 폐기한 가설이 코드 주석과 `usage.md`에 살아 있는 계약으로 남아 있었다** — "defs에 둬야 오독을 막는다". defs는 SVG 정석이라 유지할 뿐 **도구에 대한 방어가 아니다.** 폐기한 가설을 규약에 남기면 **다음 사람이 잘못된 이유로 구조를 지킨다.** **gap-detector가 두 번 만에 됐다** — 1차는 600초 무진행으로 죽었고(시작 문장만), 2차는 **범위를 넷으로 좁히고** 이미 확인한 5건을 "다시 하지 마라"로 명시해 성공했다. 좁힌 덕에 나온 A-2·B-4가 최고 발견이었다. **CodeRabbit 2건도 반복 패턴** — `lastFile` 절대경로(**세 번째**, 이번엔 스크래치패드 경로라 **사용자 이름·세션 id 포함**. 앞선 정리가 저장소 접두어만 지웠다) · `CLAUDE.md` 대조군 67 vs 실제 69(PR #35의 "54 vs 56"과 같은 자리). 검증: `verify:diagram` 5웨이브 전 항목 통과(**대조군 64 → 69** · C-20 5건) · typecheck 0 · lint diagram 0 · `verify:render` 전 항목 통과(**106초** · 글리프 구멍 항목 신설) · SSG 393/393 · **102 kB 무증가** · 육안 4모듈 전후 대조(h=16 최박 층 포함). 변경 8파일 **+774 / −61**. Act **0회**. ⚠️ **잔존 C-2가 가장 무겁다** — 106건을 낸 검사기 결함을 고쳤는데 **대조군이 없다**(`verify:render`에 자체검사 하네스 부재). 되돌아가도 아무도 모른다. Claude 초벌. |
| [render-gate-self-test](./render-gate-self-test/) | 2026-08-27 | 93% (76→92→93) | 0 | 1일(1세션) | **선행 `diagram-label-legibility`의 잔존 C-2를 닫았다** — 그 사이클이 106건 오탐을 낸 실제 검사기 결함(`NOT_PAINTED`가 배경 후보에서 빠져 마스크 구멍 `fill="black"`이 최소 포함 사각형으로 뽑히던 것)을 고쳤는데 **대조군을 남기지 못했다.** 한 줄만 지우면 조용히 되돌아가는 상태였다. 착수 전 비대칭이 심했다 — `verify:diagram` 대조군 **69** 대 `verify:render` **0**. **핵심 착상은 "서버가 필요 없다"** — `measureInPage`가 `.toString()`으로 직렬화돼 `page.evaluate`에 들어가므로 **같은 함수를** `page.setContent()`로 만든 도해에 그대로 먹일 수 있다. 대조군의 입력이 앱 상태가 아니라 대조군 안에 있어 콘텐츠가 바뀌어도 흔들리지 않는다(D-1). 색은 리터럴이 아니라 `node_modules/tailwindcss/theme.css`에서 읽어(D-2) 실제와 같은 oklch→canvas→sRGB 경로를 지난다. `--self-test`는 자격 증명 확인과 서버 탐지 **앞에서** 처리해 Chrome만으로 돈다. **대조군 11 → 14 → 15건**(⑬은 아카이브 뒤 리뷰 3회차 · E-4). Do가 11건으로 "7/7 충족"을 적었으나 Check가 *"자체검사가 통과하면서 본 검사가 깨진 상태를 만들 수 있나"* 를 묻자 **8가지가 나왔고 셋을 고쳤다** — ① **A-3 문턱이 공허**: 대비 대조군이 글자와 배경에 같은 토큰을 써 비율이 정확히 **1.00**이라 `MIN_CONTRAST`를 4.5→**1.05**로 낮춰도 11/11 통과했다(실제 사건의 1.44를 놓친다). AA 미달 + **AA 통과** 양쪽으로 고쳤다. ② **A-7 같은 병**: 글자 크기 대조군이 5px/10px라 문턱 9px 주변이 비어 있었다 → **8.8px / 9.6px**. ③ **A-1 처분 미검증**: `figures === 0` 가드를 **통째로 지워도** 11/11이 통과했다 — ⑨는 `measureInPage`의 **카운터**만 보고 그 뒤 `exit 2`는 `--self-test` 경로가 한 줄도 실행하지 않았다. **⑫ 신설**(스텁 HTTP 서버 + 자식 프로세스로 종료 코드 2와 사유 문자열 확인). 두 사이클 전 정적 관문이 종료 코드 대조군 8건을 만들며 배운 *"처분은 감지와 다른 주장이다"* 를 **렌더 쪽에서 똑같이 다시 배웠다.** **하네스를 만들다 하네스가 결함을 찾았다(D-4)** — `NOT_PAINTED` 필터가 배경 후보(`rects`)에만 있고 **라벨(`texts`)에는 없었다.** 실제 앱에서 안 터진 것은 우연이다: 패턴 글리프는 타일 좌표 (4,12)이고 층 rect는 `bodyX=16,y=16`에서 시작해 어느 rect에도 안 들어갔을 뿐이다. **fixture 대표성도 틀렸다(B ⓐ)** — 11건 전부가 `<figure style="background:#fff">`를 줘 `frameBg.a > 0` 갈래를 지났는데 실제 `DiagramFrame`은 `className="not-prose my-6"`뿐이라 **실제 도해가 절대 안 지나는 경로**였다. **FR-7을 문언대로 채웠다** — Do는 판정 **6종**만 되돌리고 "6종 전부"라 적었으나 FR-7은 *"**각** 대조군"* 이다. **14건 전부** 되돌려 각각 해당 대조군만 깨지는 것을 확인(**14/14**). ①이 처음 "안 깨졌다"고 나온 것은 판정이 공허해서가 아니라 **되돌림이 틀려서**다 — `out.figures += 1;`(137행) 뒤에 주입했는데 `cap`은 **138행에서 선언**돼 TDZ로 `page.evaluate`가 통째로 터졌고 `❌ 실패` 줄이 하나도 안 나왔다. **구분법을 남겼다: 대조군이 공허하면 그 하나만 조용하고, 되돌림이 틀리면 전부 조용해진다.** **세 번째 재귀 함정** — 자식이 자체검사를 또 돌려 무한 재귀(10분 타임아웃), `DGM_RENDER_CHILD` 가드로 끊었다. `--all`·`--assert-only`에 이어 세 번째다. `spawnSync`가 부모 이벤트 루프를 막아 스텁 서버가 응답 못 한 것도 겪었다(종료 코드는 2로 맞았지만 **이유가 달랐다** → 비동기 `spawn`). PR #38 리뷰 3건 반영(문서 수치 낡음 · FR-7 기록 6/14 · **좀비 Chrome** — `tok()` 실패 시 `process.exit(2)`가 `finally`를 건너뛰므로 fixture 생성을 **브라우저 기동 앞으로**; Chrome 30→30 확인). **비용은 정직하게 적었다** — 106.57초 → **152.81초**, `--self-test` 단독 **49.58초**. Design D-3의 *"하네스가 0.3초라 아낄 이유가 없다"* 는 **근거가 빗나갔다**: ⑫ 혼자 46초로 자체검사 비용의 92%다(백로그 D-2). 실행 시간 표기를 이 사이클에서 **세 번** 고쳤다(3분→1.8분→2.5분) — 앞의 둘은 낡은 값, 마지막은 **내가 늘린 값**. 남은 것: **A-2(검사 범위에 하한이 없다 — `SVG_COMPONENTS`에서 컴포넌트가 빠지면 그 페이지가 통째로 검사 밖인데 `exit 0`. `verify:diagram`의 `WAVES`+D-10 대응물이 없다 · 가장 큰 구멍)** · A-4(`SLACK` 문턱이 안 잡힘) · A-5(불투명 well이 맨 위인 형상 무대조군) · A-6(알파 합성 무대조군) · A-8(색 해석 실패 분기 무대조군) · B ⓑ(k 분포 비대표) · B ⓒ(`<tspan>` 무대조군) · **D-2(⑫ 비용)**. |
| [render-gate-scope-floor](./render-gate-scope-floor/) | 2026-08-28 | 94% (Do 시점 대비 Check 9건) | 0 | 1일(1세션) | **선행 `render-gate-self-test`의 잔존 A-2·E-5를 닫았다** — 그 사이클이 *"가장 큰 구멍"* 이라 적은 것이다. `verify:render`는 검사할 페이지를 **파일시스템에서 유도**하는데(`src/components/diagram/*.tsx` 중 `<svg` 포함), 유도가 조용히 틀리면 **범위가 줄어든 채 "전 항목 통과"** 가 나왔다. 하한 단언이 `SVG_COMPONENTS.length === 0`·`!urls.length` 둘뿐이라 **0일 때만** 울었다. **착수 전 실측**: 도해 12종 중 SVG 판정 6종 · 검사 URL 92개 · `LayerStack`이 빠지면 **−36개(39.1%)** · `NodeGraph` −33 · 나머지 −6/−5/−2/−1 — **어느 경우에도 종료 코드 0**. **해법은 DOM 신원 교차 확인**(사용자 선택) — 도해가 `data-diagram`으로 자기 종류를 남기고, 관문이 **유도(기대)** 와 **실제 렌더(관측)** 를 대조한다. 목록 등록(`WAVES`식)을 안 쓴 이유는 스크립트가 이미 반대 근거를 적어 뒀기 때문이다(*"목록을 적어 두면 7번째 SVG 컴포넌트가 생겼을 때 조용히 건너뛴다 — 수동 미러를 늘리는 일"*). 그리고 레지스트리는 *"적힌 것이 다 있나"* 만 보고 *"적히지 않은 것이 빠졌나"* 는 못 본다. **미러가 나쁜 게 아니라 대조되지 않는 미러가 나쁘다.** **설계가 실측으로 세 번 고쳐졌다.** ① `data-diagram` 값 — `LayerStack.name`은 프로덕션에서 **`"J"`** 가 된다(Next.js가 서버 번들도 최소화). 리터럴을 쓴다. ② **γ의 기준선이 공허했다** — Design이 정한 *"MDX가 쓰는 종류"* 집합이 유도와 **같은 목록**에서 만든 정규식으로 채워져 판정이 절대 참이 될 수 없었다. 배럴 `index.ts`로 교체(이름과 경로를 따로 적어 파일이 밖으로 나가도 이름이 남는다). **Design 위험표가 이 위험에 이름을 적어 뒀는데 거기 적은 대응이 바로 그 함정이었다.** ③ **δ 신설** — 아래. **★G-5를 대조군보다 앞에 둔 판단이 사이클을 구했다.** 1차 구현(α·β·γ)으로 `LayerStack`을 유도에서 빼니 92→56페이지인데 **`✅ 전 항목 통과`** 가 나왔다 — 남은 56 중 **8개에 `LayerStack`이 함께** 있어 관측에는 나왔기 때문이다. α·β·γ는 *"종류가 한 번이라도 그려졌나"* 만 본다. **δ(페이지 단위 피복)** 를 넣자 `exit 2` + `δ LayerStack — 페이지 36개가 검사 목록에 없다`(착수 전 실측치와 일치). **대조군을 먼저 만들었다면 전부 통과하는 채로 36페이지가 검사 밖인 사이클로 끝났을 것이다.** **관문이 실물로 둘을 잡았다** — **F-1** `FlowSteps`가 `lucide-react` 아이콘으로 `<svg viewBox>`를 그리는데 파일에 `<svg` 리터럴이 없어 유도가 비SVG로 분류(δ 기준을 *"글자 있는 SVG"* 로 좁혔고 이 시나리오가 그대로 ㉒ 대조군이 됐다). **F-2** `chapters.json`에 `number` 필드가 **0개**인데 매핑이 `String(c.number).padStart(2,"0")`을 써서 `slugByNum`이 **항상 빈 Map** — 책 17장이 URL 매핑을 못 받았다. **A-2의 실물**이다(경고 한 줄 + `exit 0`). `id` 기반으로 고쳤다. **Check가 9건을 더 찾았고 전부 고쳤다.** 최상위는 **C-3**(gap-detector 추적 → 실측 확인): *"두 독립 출처"* 가 실은 하나였다 — `collectUrls()`의 `if (!kinds.length) continue`가 배럴 정규식을 `urls`의 **상류**로 만들어, 배럴에서 `NodeGraph`를 빼면 URL이 **92 → 87**로 조용히 줄고 δ는 그 종의 키가 없어 침묵했다. `urls`를 배럴에서 떼고 **ε(디렉터리 − 배럴)** 를 세웠다 — 배럴 정규식이 놓친 경우(`export { A, type B } from`)도 여기 걸린다. **C-1** δ의 **배선**이 무대조군이었다(`pagesByKind`를 빈 Map으로 바꿔도 자체검사 통과) → **㉒ 신설**. **C-9** C-6에서 목록 미러를 지웠는데 **같은 함수 두 줄 아래** 문자열 미러(`/α LayerStack/`)가 남아 G-5 조작 시 자체검사가 먼저 실패해 **δ의 증거를 가렸다.** 그 밖에 C-4(정적 판정을 브라우저 앞으로) · C-5(σ 신설 — `toUrl` null이 `exit 0`이던 F-2의 형제) · C-2(α 메시지 분기) · C-6 · C-7(페이지 오류가 범위 판정에 삼켜짐) · C-8. **최종 판정 일곱 방향** — α(MDX가 쓰는데 안 그려졌다) · α₀(어느 MDX도 안 쓴다) · β(그려졌는데 파일이 없다) · γ(배럴은 아는데 파일이 없다) · δ(**페이지가 검사 목록에서 빠졌다**) · ε(파일은 있는데 배럴이 안 내보낸다) · σ(URL 규칙이 없다). **γ·ε·α₀·σ는 브라우저를 띄우기 전에** 판정한다 — 예전엔 110초를 헛되이 쓰고 울었다. **대조군 15 → 28건**(판정 14 · 범위 10 · 처분 4). **값은 오히려 줄었다** — 자체검사 **49.58초 → 6.82초**, 본 검사 **152.81 → 112.66초**. 처분 대조군을 **임시 cwd**에서 돌린 덕이다(`collectUrls`가 cwd 기준이라 URL이 저절로 줄어든다). **범위 축소 인자를 만들지 않고** 얻었다 — 그런 인자는 이 사이클이 막으려는 위험 그 자체다. 선행 백로그 **D-2**(⑫가 자체검사 비용의 92%)가 함께 닫혔다. **교훈**: *"독립 출처"라고 적었다고 독립이 되지 않는다 — 독립성은 주장이 아니라 데이터 흐름의 성질이다* · *실물로 한 번 울려 보는 것이 대조군 열 개보다 앞선다* · *순수 함수로 뽑으면 대조군이 싸지지만 **배선**이라는 새 무대조군 지대가 생긴다* · *"혼자만 깨지는 대조군"은 잘 격리된 것이 아니라 본 검사와 이어져 있지 않다는 뜻일 수 있다* · *미러는 종류가 여럿이라 하나를 지워도 남는다(목록·문자열·정규식·경로)* · *같은 교훈이 자리를 옮겨 다시 온다 — A-1(감지 대 처분) → E-5 → C-1(감지 대 **배선**)*. 남은 것: **B-1(β의 배선 무대조군 — C-1과 같은 모양이 하나 남았다)** · **B-5(정적 판정이 자식 대조군 앞을 막아 메시지가 원인을 안 가리킨다 — C-9가 그 실물)** · B-2(스텁 마크업이 실제와 다르다) · B-3 · B-4 · F-2 잔재(`number` 부재의 다른 소비자는 안 봤다) · 선행 A-4·A-5·A-6·A-8·B ⓑ·ⓒ 유지(식별자 충돌을 피해 출처를 붙였다 — 보고서 §7 참고). |
| [render-gate-control-claims](./render-gate-control-claims/) | 2026-08-29 | 97% | 0 | 1일(1세션) | **선행 `render-gate-scope-floor`의 잔존 B-1·B-5를 닫았다.** 판정을 순수 함수(`scopeStatic`·`scopeViolations`)로 뽑아 대조군을 싸게 얻었는데 **그 함수를 부르는 자리**는 아무도 안 봤다. **착수 전 실측이 B-1을 5배로 키웠다** — 호출부 인자 11자리를 하나씩 훼손하니 **5자리를 망가뜨려도 자체검사가 종료 0**이었다(`scopeStatic`의 svg·dir·barrel·skipped + `scopeViolations`의 dir). 사용자가 지목한 B-1은 그중 하나였고, **브라우저 앞 판정 전체(γ·ε·α₀·σ)가 사실상 무대조군**이었다. 선행 Check가 *"배럴 조작 실측이 γ·ε를 덮는다"* 고 적은 것도 **틀렸다** — 배럴 **파일**을 조작하면 잡히지만 **배선**을 조작하면 안 잡힌다. **B-1과 B-5는 같은 구조에서 나온다** — 자식이 임시 cwd에 실제 저장소를 심링크로 잇고 정적 판정이 자식보다 앞에 있어, (a)정적 배선을 무력화하면 자식이 그냥 지나가고 (b)정적 판정이 울면 자식이 그 메시지로 죽어 노린 사유와 어긋난다. **해법은 자식을 저장소에서 떼어 내는 것**(사용자 선택) — 임시 cwd에 **가짜 도해 트리**(컴포넌트 + 배럴 + MDX)를 직접 심으면 γ(배럴에만 있는 Ghost)·ε(파일만 있는 Extra)·α₀(아무도 안 쓰는 Lonely)·σ(URL 규칙 밖 topics/)·β(없는 종을 그리는 스텁)를 **직접 유발**할 수 있다. `node_modules`만 빌린다 — 검사 대상이 아니라 **실행 수단**이다. **⑳ 하나만 실제 트리를 남겼다**(전부 가짜면 *"유도 규칙이 실제에서 도는가"* 를 아무도 안 보게 되고 그것이 이 사이클이 고치는 병과 같은 모양이라 · Design D-4). **★한 걸음마다 감사를 돌린 것이 값을 했다**: ①감사 신설 6/11 → ③가짜 트리+자식 4 이사 **6/11**(격리는 새 판정을 안 만든다 · 예상대로) → ④㉔ 정적 4종 **10/11**(자식 하나가 정확히 네 자리를 덮었다) → ⑤㉕β·㉖음성 **11/11**. 한꺼번에 만들고 11/11만 봤다면 **어느 대조군이 어느 자리를 덮는지 몰랐다.** **설계에 없던 것 둘**: `scopeViolations.dir → barrel` 훼손은 **관측 불가**다(γ·ε가 `dir ≡ barrel`을 보장하므로 그 검사를 통과한 실행에서 둘은 같은 값). **11/11을 만들려고 억지 대조군을 붙이는 것이 아니라 훼손을 관측 가능한 형태(`dir: []`)로 바꾸는 것이 맞다 — 덮을 것이 없는 자리를 무대조군으로 세면 감사 수치가 거짓말을 한다.** 그 과정에서 **㉖(음성 자식)** 이 없다는 것도 드러났다(처분 대조군이 전부 `exit 2`를 기대하면 **과잉 발화가 안 보인다**). 그리고 FR-8 되돌림 8건 중 **P-1만 아무도 안 깼다** — ㉗을 더해 8/8. **시제품이 결함 둘을 예고했다**: P-1 `!urls.length` 가드가 `scopeStatic`보다 먼저 돌아 σ 목록 대신 뭉뚱그린 메시지가 나온다(앞으로 옮겼다) · P-2 `stubFigures`에 배경이 없어 **대비 오탐 2건**이 나는데 **앞선 `exit 2`에 가려져** 있었다 — 판정 fixture의 `wrap()`은 이미 배경을 주고 있었다(**한 곳에서 피한 함정이 다른 곳에서 되살아났다**). **★Check가 일곱을 찾았고 다섯을 고쳤다 — 감사 자체가 손으로 적은 미러였다.** C-1 **분모가 스스로 준다**(`CASES`에서 σ 훼손을 지우니 `10/10 · ✅ 전부 덮인다`) → 호출부에서 인자를 **유도해 대조**. C-2 그 대조를 넣은 **첫 실행**이 짚었다 — `scopeViolations.barrel`이 훼손 목록에 없었고 **본문에서 안 쓰이는 죽은 인자**였다(11/11이 실은 11/12). C-3 **분자**가 종료 코드만 보고 세어진다 — `pagesByKind`를 비우면 α₀를 주장하는 ㉔은 **통과**하고 다른 것들만 부수적으로 깨졌다 → `wantBroken`(주장 대조군) + `denyReason`(과잉 발화 금지). C-4 D-5 면책이 **거짓으로 떴다**(자식 출력 문구로 판단) → `realTreeScopeViolations()`로 **저장소를 직접 본다**. C-5 소스를 고치는 도구가 `Ctrl-C`에 취약(SIGINT는 `finally`를 안 돈다) + 백업 이름 고정 → 시그널 원복 + pid. **★B-8 정정 — Design D-4의 전제가 틀렸다.** 유도 규칙(`<svg` 필터)을 **통째로 지워도 ⑳은 통과**한다(fixture MDX가 `SVG_COMPONENTS`에서 파생되고 스텁이 **위치**만 쓰기 때문). 그 훼손을 실제로 잡는 것은 **가짜 트리 자식 ㉒·㉓**다. ⑳의 값은 *"α 배선 + 저장소 정적 건강검진"* 이다. **PR #40 리뷰가 3라운드에 4 → 2 → 1로 수렴했다.** 1회차 최상위는 **탐지기가 `return scopeStatic({…})`을 놓친 것** — 정규식이 대입문만 찾았고, **그 자리는 C-4를 고치며 내가 만든 것**이다(탐지기를 만든 사람이 탐지기의 사각에 코드를 놨다). 2회차는 **이중 감점**(B-8이 축을 95로 내린 근거인데 총점에서 또 뺐다 → 96.65 반올림 97%), 3회차는 **시간 수치의 출처**(Do 마감 14.48/121.57초와 Check 반영 후 14.68/121.86초는 다른 실행이다 — 같게 만들지 않고 밝혔다). **1회차의 "산식을 재현 가능하게"가 2회차의 이중 감점을 드러냈다.** **같은 것을 다섯 번 만났고 매번 "고치려고 만든 도구" 안에서였다** — 덮을 것 없는 자리(Do) · 무대조군 P-1(Do) · 분모(C-1) · 분자(C-3) · 탐지기 사각(PR #4). 대조군 **29 → 33건**(판정 14 · 범위 10 · 처분 9). 새 명령 `npm run audit:wiring` — **관문이 아니라 관문을 재는 자**(약 2.5분 · `scripts/audit-render-wiring.mjs` 229줄). **교훈**: *재는 자도 재어야 한다* · *분모와 분자를 모두 붙잡아야 지표다* · *덮을 것이 없는 자리와 안 덮인 자리는 다르다* · *미러는 자리를 옮겨 다시 나타난다(목록→문자열→정규식→경로→훼손 목록, 다섯 번째)* · *"적어도 이것"과 "정확히 이것"은 다르다* · *면책은 증거로 한다* · *소스를 고치는 도구는 `Ctrl-C`를 견뎌야 한다* · *수치는 실행 단위로 적는다*. 남은 것: **B-8(D-4가 ⑳에 맡긴 주장을 지는 대조군이 없다)** · **B-10(ζ 표본이 끌어들인 페이지의 `pageOverflow`가 무대조군)** · B-12(자체검사 진단 경로는 감사 밖 — 이유와 함께 출력한다) · B-6·B-7·B-9·B-11 · 선행 백로그 유지. |
| [render-gate-derivation-claim](./render-gate-derivation-claim/) | 2026-08-30 | 90% (자가 91→80→86→90) | 3 | 2일(1세션) | **선행 `render-gate-control-claims`의 잔존 B-8·B-10·B-12를 닫았다.** `verify:render`의 **검사 범위**는 유도 규칙(`*.tsx` 중 `'<svg'` 포함)이 정하는데, 그 규칙을 훼손하면 관문이 울기는 울어도 **우는 대조군이 그 규칙을 주장하지 않았다** — ㉒㉓이 깨지는 것은 가짜 트리의 `Extra`가 오분류되는 **부수 효과**였고, 누가 그 fixture를 건드리면 보호가 조용히 사라진다. **착수 실측이 설계를 뒤집었다**: ㉖에 `유도 1종`을 요구해도 필터 삭제를 못 잡았다 — `CLEAN` 트리에 `Alpha` 하나뿐이라 **거를 것이 없는 트리에서는 거르는 규칙을 검사할 수 없다.** 그래서 ㉘의 트리에 `<svg` 있는 종과 없는 종을 **둘 다** 뒀다. **그런데 Check가 그것도 부족함을 밝혔다** — SVG 1 + 비SVG 1은 **대칭**이라 필터를 반전해도 유도 결과가 1↔1로 같고, 배너 `유도 1종`이 성공 쪽에도 뜬다. 실제 판별자는 `wantStatus: 2`였고 **요구는 장식이었다**(9조합 실측). 비대칭(SVG 2 + 비SVG 1)으로 바꾸고 요구를 배너 하나로 줄였다. **★이 사이클은 자기 자신에게서 같은 병을 다섯 번 더 찾았다** — ①㉚의 `denyReason`이 장식(δ 출력 경로가 exit 2라 `wantStatus: 0`이 언제나 먼저 걸린다 — 실측·구조 양쪽 확인) ②`RULES_MIN = 4`인데 행이 5라 **장치가 태어나면서 한 칸 헐거웠다**(한 행을 지워도 `3/3`으로 초록) ③호출부 정규식이 `scopeX({` 형태만 봐서 호출 5곳 중 3곳만 세고 **가드가 자기 눈으로 초록을 냈다** ④고아 백업 감지가 **동시 실행의 안전망을 조용히 지웠다**(위험을 줄이러 온 장치가 위험을 키웠다 — 이 사이클 최악) ⑤`RULES_MIN`의 아래 한 겹, **분모의 구성**이 무방비(주장 행을 가드로 옮기면 조용히 준다). **그리고 4회차가 그 반대편을 찾았다** — `selfTest()`가 `r.signal`·`r.error`를 버려서, `Ctrl-C`로 자식이 죽으면 `status=null`이 되고 감사가 **덮인 항목을 `무대조군`으로** 보고한다. 내내 *거짓 초록*을 쫓았는데 **거짓 빨강**이 있었다. `die()`가 아니라 **throw**로 고쳤다 — `process.exit`는 `finally`를 건너뛰어 소스를 훼손된 채 남긴다. 고치자마자 내가 `timeout 20`으로 감사를 죽였을 때 **즉시 실증**됐다. **세 번 같은 함정에 빠졌다** — 관문이 `35건·exit 2`를 냈는데 세 번 다 **소스가 훼손된 순간**을 읽은 것이었다(배경 감사 중 실행 · `kill -9` · `timeout`). `SIGKILL`뿐 아니라 **`SIGINT`·`SIGTERM`도 본문 중엔 안 먹는다**(`await` 0개 + `spawnSync` 블로킹 · `timeout 25`가 5분 43초 생존). 주석은 반대로 단언하고 있었고 **그것이 조급한 손을 `kill -9`로 보낸다.** 운영 규약을 `CLAUDE.md`로 승격했다. **무한 후퇴는 없다** — `CASES`는 호출부에서 **유도**하고 `RULES`는 유도할 원천이 없어 손으로 적되 **유도값과 `!==`로 대조**한다. A-1의 병은 *"손으로 적었다"* 가 아니라 *"아무와도 대조되지 않았다"* 였다. 그 원칙대로 `RULES_MIN`도 `RULE_CLAIMS + RULE_GUARDS`에서 유도로 바꿨다(손 숫자 셋→둘, 이때 TDZ를 만들었다가 순서 확인으로 잡았다 — `node --check`는 실행 순서를 안 본다). **Check가 네 번, 매번 다른 판독자가 봤다** — 자가 91% → 80 → 86 → 90. **91→80이 가장 정직한 수다**: 분모를 스스로 정하면 점수는 언제나 후하다. 3회차 판독자는 내가 고치기 전 코드에서 고아 감지 결함 둘을 **독립적으로** 찾았다 — *읽으면 찾힌다*가 두 번 재현됐지만 **대조군도 감사도 그 둘을 못 잡는다**(D-7). **수치**: 대조군 33 → **36**(판정 14·범위 10·처분 12 — 열둘 중 열하나가 가짜 트리, ⑳만 실제) · 감사 배선 11/11 · 규칙 **0/2 → 4/4**(+가드 1/1) · 항목 11 → 16 · 자체검사 14.68 → 약 23초 · 감사 **372~394초**(한 수로 안 적는다, 실행마다 20초 넘게 갈린다). **감사 = (배선 + 규칙) × 자체검사** — 대조군의 비용은 곱셈이다. **G-6은 ❌ → 부분 달성**: `한 종만 제외` 훼손이 선행 G-5(`LayerStack` 하나만 제외)를 **서버 없이** 재현하고 ㉘이 단독 포착한다. 다만 가짜 트리 안(R1)뿐이고 **R2(실제 컴포넌트의 부분 오분류)는 여전히 본 검사만 본다 — B-8은 절반만 닫혔다.** **남긴 것**: A-3 잔여 + D-1~D-13. 가장 무거운 셋 — **D-2** 자체검사 자신의 분모가 무방비(fixture를 지워도 전 항목 통과 · **그래서 35→36이 됐는데 아무도 안 울었다** · Act에서 열어 보고 접었다, 출력 자리가 넷이라 한 줄이 아니다) · **D-5** R2 · **D-7** 새로 넣은 장치는 무대조군으로 태어난다(감사 자신을 재는 자가 없다). **교훈**: 교훈을 적는 것과 적용하는 것은 다른 일이다 — 1회차 지적을 반영하는 그 커밋 안에서 같은 병을 새 장치 셋에 만들었다. **교훈은 문서가 아니라 검사로 남겨야 한다.** 그리고 **검사 결과가 빨간 것이 검사 대상이 틀렸다는 뜻은 아니다** · **줄번호는 코드의 미러이고 대조되지 않는 미러다**(인용한 줄이 거의 다 밀려 있어 코드 주석에서는 앵커로 바꿨다). |

---

## 기록 정책

- 아카이브 후에도 `.bkit/state/pdca-status.json`의 `features`에는 항목을 남기고 `phase: "archived"` + `archivedTo`를 기록한다 (이 저장소 관례 — 2026-05 이후 전 사이클 동일).
- Plan/Design/Analysis/Report 문서는 `docs/archive/YYYY-MM/{feature}/` 폴더로 이동한다.
- **지속 참조 자산은 아카이브하지 않는다.** `diagram-component-set`의 `usage.md`(12종 사용 규약)와 `baseline.md`(97모듈 자수 고정값)는 `docs/02-design/features/`에 남긴다 — CLAUDE.md와 선행 사이클 Design §5.1·§5.2가 이 경로를 참조하고, 후속 자료원 저작자가 계속 봐야 하는 살아 있는 문서이기 때문이다.

## 후속 백로그 (first-semiconductor-primer 파생)

- ~~**Design §5.1 분량 기준 현행화**~~ → ✅ **2026-08-09 완료**. 항목 1,200~2,000 → **1,200~2,300**, COLUMN 600~1,200 → **600~1,600**. 갱신 과정에서 수치 외 2건이 더 드러나 함께 반영했다: ① 설계는 **Easy(600~1,000자)를 주력**으로 봤으나 실물은 `LayeredExplain`이 도입부(Easy 평균 168자)이고 **`##` 본문 섹션이 주력(평균 1,173자)** — 설계 표에 아예 없던 층이라 행을 신설했다. ② report(평균 1,837·최대 2,341)와 analysis(평균 1,753·최대 2,274)의 수치 불일치는 **측정 기준 차이**(마크다운 기호 포함 여부)여서, §5.1에 측정 방법을 **공백·마크업 제외**로 못 박았다. 그 기준에서 88항목 전량이 새 범위 안. (Design §0 V-6 · §5.1 / Plan §4.1 주석 / Report §4.1·§8.2)
- **공개 전환 시 판권 재확인**: 현재 종결은 **비공개 운영이 전제**다. 로그인 게이트를 해제하면 역자·출판사·발행연도 확인이 다시 공개 전 게이트가 된다 (Report §4.2).
- **자체 다이어그램 확대 검토**: FR-9는 "필요 최소" 기준으로 충족(표·목록으로 대체)했으나, 소자 구조·공정 흐름은 도해가 있으면 이해도가 오른다. 특히 2장(소자)·7장(전공정).
- **입문 → 심화 동선 재검토**: 97모듈 입문 계층이 새로 생겼으므로 홈의 "여기서 시작" 진입점과 `RoleQuickEntry` pill 구성을 다시 볼 여지. `mainpage-restructure`(2026-07) 이후 자료원이 12→13개로 늘었다(이후 cert-equip-maintenance 병합으로 14개).
- **기술 서술 사람 검수**: CodeRabbit이 16건을 잡아낸 데서 보이듯 원서(2010년경) 서술에는 현재 기준과 어긋나는 대목이 더 있을 수 있다. 특히 9장(최첨단)과 4장(메모리)은 이후 전개가 큰 분야다.
- **Gap 판정 스크립트에 양성 대조군**: 이번 사이클에서 자기 검증이 4번 틀렸다. 판정 스크립트에 의도적으로 틀린 입력을 함께 넣어 **스크립트 자체를 검증**하는 절차가 필요하다. Match Rate는 그룹 소계 합과 총계를 교차 검증한다.

## 후속 백로그 (cert-equip-maintenance 파생)

- **photo-process 노광 광원 파장값 보강**: g-line/i-line/KrF/ArF 등 구체 파장 수치가 원문 슬라이스에 텍스트 근거 없이 그림 캡션으로만 존재해 미보강(창작 금지 원칙). 원본 PDF 재확인 또는 타 신뢰 자료로 보강 가치 있음.
- **태그 변별력 재검토**: automation-plc·electrical-facility·environment-management 3개 모듈이 `engineering-controls` 단일 태그라 cross-link 관련자료 집합이 유사. 본문이 확장되면(백로그 하단) 2번째 축 재검토.
- **첫 자료원(입문서) ↔ 수험서 상호 연결 검토**: first-semiconductor-primer(13번째, 개념 입문)와 cert-equip-maintenance(14번째, 자격증 실무)가 같은 사이클에서 독립적으로 병합돼 서로를 모르는 상태 — cross-link 상호 태깅 여지 있음(후속 사이클).

## 후속 백로그 (diagram-component-set 파생)

- ~~**G-1·G-3 육안 확인**~~ → ✅ **2026-08-13 종결** (`diagram-expansion` G-9). 세 사이클 연속 이연된 유일한 미완 게이트였고, 닫는 순간 **정적 검사가 볼 수 없는 결함 3건**이 나왔다. 확인 방식은 사람 육안이 아니라 **헤드리스 Chrome 계측**으로 바뀌었다 — 로그인→뷰포트 지정→요소 캡처 + 실효 글자 크기·대비·가로 넘침 측정. 판정 결과:
  - **`007`·`008` band 모드** — ⓐⓑⓒ 통과(각진 모서리·에너지 축 겹침 없음·금지대역 점선이 빈 공간으로 읽힘). ⓓ **밴드 tone은 다크 모드에서 문제없었다**(전도 13.2:1 · 충만 14.6:1) — "세트에서 대비가 가장 약한 조합"이라는 예측은 **틀렸다**. 실제로 AA에 미달한 것은 예상 밖의 `accent`(3.36 / 3.97)와 칠해진 노드 위의 `TEXT_MUTED`(4.01)였다.
  - **파일럿 5모듈·`055`·`080`** — 별도 확인 대상이 아니라 **사이트 전체 SVG 94페이지 전수 스윕**에 포함해 처리했다(가로 넘침 0 · 실효 글자 9px 미만 0).
  - **예측하지 못했던 결함이 가장 컸다** — 375px에서 SVG 도해가 ×0.54로 눌려 라벨이 5.9~7.0px가 되는 문제는 **109 인스턴스 / 94 모듈**에 걸쳐 있었고, 이 백로그 항목의 "375px 조건"이 가리키던 것은 `CompareCards` 압착이었으나 **압착은 일어나지 않았고**(`sm` 미만은 카드 목록으로 재배치) 진짜 결함은 SVG 축소였다.
- ~~**`LayerStack.orientation('band')` 미구현**~~ → ✅ **2026-08-11 완료** (`diagram-set-finishing`). 단순 미구현이 아니라 **부정확 교정**이었다 — 007이 전도띠에 `silicon-n`, 금지대역에 `oxide` tone을 빌려 써 도핑 도해로 오독될 여지가 있었다. band 모드(각진 모서리·에너지 축·점선 금지대역·채움 패턴 미적용) + 밴드 전용 tone 3종 신설.
- ~~**`Timeline.span` 미구현**~~ → ✅ **2026-08-11 시그니처 삭제**. 사용처 2곳이 전부 이벤트 목록만 쓰고 세로 목록 렌더에는 시간 축이 없어 현행 모델과 맞지 않았다 (YAGNI).
- ~~**도해 글자 크기 상대 단위**~~ → ✅ **2026-08-11 계약 확정(C-1)**: viewBox 절대 단위 유지, 본문 토글과 미연동. 근거 — SVG 내부에서 `em`은 부모의 CSS px를, viewBox는 사용자 좌표계를 쓰므로 섞으면 375px에서 라벨이 도형을 넘친다. 접근성은 브라우저 확대와 표 병치로 확보.
- ~~**FR-8 `altTable` 판정 확정**~~ → ✅ **2026-08-11 계약 확정(C-2)**: **표 병치를 원칙**으로 하고 `altTable`은 표를 실제로 대체할 때만. FR-8을 "표 병치 또는 altTable 중 하나"로 재정의했고, 자수 예외의 근거 문장도 두 곳 모두 정정.
- ~~**tokens 우회 2건**~~ → ✅ **2026-08-11 완료**. `KIND_FILL`을 `tokens.ts`의 `NODE_KIND`로 이관(노드 종류는 재료와 다른 축이라 `TONE`과 병합하지 않고 나란히), `LatticeDiagram`의 리터럴 2곳은 `TONE.substrate.fill` 참조로.
- ~~**`<desc>` 0건**~~ → ✅ **2026-08-11 완료**. SVG 6종 전부 **데이터에서 자동 생성**(저작자 입력 0 → 누락 불가). `aria-labelledby`는 6종에 내부 id를 요구해 V-3의 `idPrefix` 2종 축소와 충돌하므로 `aria-label` 유지.
- ~~**도해 세트의 타 자료원 확대**~~ → ✅ **2026-08-13 완주·아카이브** (`diagram-expansion` — 4웨이브 156모듈 349 인스턴스, D-2 전수 129/129 충족, G-9 종결). 아래는 착수 시점 기록이다. `diagram-set-finishing`이 C-1·C-2 계약을 고정한 뒤 안전하게 착수했다. **백로그의 "213모듈" 전제가 실측으로 정정됐다** — 213이 균일한 대상이 아니다. `ncs-semi` 84모듈은 이미 원문 도판 345장(4.1/모듈), 책 17장은 80장(4.7/모듈)을 갖고 있어(`ncs-image-expansion` 등이 넣은 것), **진짜 시각 자료 0인 모듈은 129개**다(`hs-*` 8종 96 + 수험서 13 + daegu 10 + osha 10). `first-semiconductor`가 도해를 받은 이유는 "글만 있어서"가 아니라 원문 도판 336개를 저작권상 전면 미사용했기 때문이므로, 도판 보유 101모듈에 균일 적용하면 같은 내용을 두 번 그리는 일이 된다. → **W1 완주(2026-08-12): 교과서 4권 45모듈 · 102 인스턴스 · 전수 커버리지 45/45.** 남은 웨이브 — W2 교과서 잔여 4권+daegu 61모듈 · W3 수험서+OSHA 23모듈 · W4 도판 보유 101모듈(W3 후 재판단). ※ **여기의 102는 Do 종료 시점 값이다** — Check가 보조 도해 7건 미이행을 적발해 보완한 뒤 **W1의 최종값은 109**이고, 누계 349(=109+146+60+34)는 그 값을 쓴다. 이 줄은 2026-08-12 기록이라 원문을 남긴다.

## 후속 백로그 (diagram-set-finishing 파생)

- **`ToneStyle.dashed?` 추상화 (L-2)**: band 렌더 분기가 tone 문자열에 하드코딩돼 있다 — `layer.tone === 'band-gap'`. F-5가 색 리터럴을 tokens로 올린 직후 **스타일 분기가 tone 키에 결합**된 셈이라 일관되지 않는다. 지금 보류한 이유는 밴드 tone이 3종뿐이고 분기가 1곳이라 **추상화하면 과설계**이기 때문 — 밴드 계열이 늘면 그때 `ToneStyle`에 속성으로 올린다.
- **`ARROW_FILL` 상수 (L-3)**: band 축 화살촉의 `fill-slate-400 dark:fill-slate-500`이 `NodeGraph` 화살표와 문자열이 같은데 리터럴로 남았다. `STROKE`의 fill 짝이라 상수가 필요하지만 사용처가 2곳뿐이어서 보류.
- **`<desc>` 문안 규칙 현행화**: 구현 3건(`LayerStack`·`NodeGraph`·`LatticeDiagram`)이 Design §5 표 형식과 미세하게 다르나 **셋 다 구현이 더 정확**해 Gap이 아니라 개선으로 재분류했다(`LatticeDiagram`은 `highlight`가 없어도 원자가에서 상태를 유도하므로 누락이 없다). 다음에 §5 표를 손댈 때 구현 문안을 정본으로 삼는다.
- **Match Rate 검증 항목에 DoD를 포함시킬 것**: 이 사이클은 Plan DoD 7항목 중 1항목("육안 확인 목록 정리")이 Check까지 미이행이었는데, **48항목 검증 매트릭스에 그 DoD가 항목으로 들어 있지 않아** Match Rate 97.3%가 미이행을 가리지 못했다. Report 단계에서야 발견했다. 다음 사이클은 **DoD 각 항목을 검증 매트릭스에 1:1로 올린다**.
- **규칙 문장의 복제 추적**: M-3은 같은 규칙 문장이 두 문서에 복제돼 있어 한쪽만 고친 사고였다. 정정 대상을 셀 때 **문장 단위가 아니라 규칙 단위로 검색**해야 한다.

## 후속 백로그 (diagram-expansion W1 파생 · 2026-08-12)

- **`tone` 13종이 부족하다**: 공핍영역·광학층·질화막용 tone이 없다. W1에서 공핍영역은 두 층 + 경계 `annotation`으로, CIS 마이크로렌즈·컬러필터는 `oxide`·`resist`로 근사했다. `band-gap`을 공핍영역에 빌려 쓰면 tone 오용이다(`band-*`는 에너지 축 전용 — `diagram-set-finishing`이 고친 바로 그 실수). **PN 접합은 `diode`·`mosfet`·`bjt`·`electronic-devices` 4모듈에 반복 등장**하므로 `usage.md` §4 승격 기준(3모듈 이상)에 이미 걸린다 — `depletion` tone 신설을 검토할 시점.
- ~~**`CompareCards`에 `scrollable`이 없다**~~ → ✅ **2026-08-13 판정 종결** (G-9 실측). **`scrollable`은 필요 없다** — `CompareCards`는 `sm:hidden` / `hidden sm:block` 두 렌더를 갖고 있어 375px에서는 **카드 목록으로 재배치되므로 압착 자체가 없다**(최대 사례 3열×11행 `chemicals-usage`로 확인). 압착은 `sm`(640px) **이상**에서만 생기고 3열의 최악값이 640px에서 데이터열 147px(768px 189 · 1440px 227)로 읽을 수 있다. 4열이면 640px에서 116px로 떨어지므로 **"2~3열을 지킨다"는 규약이 실제 방어선**이다. 같은 확인에서 **이 항목의 "3종만 넘긴다"는 서술도 틀렸음**이 드러났다 — `ScaleRuler`·`TruthTable`이 이미 넘기고 있었고, G-9에서 `LayerStack`·`LatticeDiagram`을 더해 **SVG 6종 + `TruthTable` = 7종**이 되었다.
- **`FlowSteps.loop.from`/`to`가 구현에서 해석되지 않는다**: `loop.label`만 렌더한다. `diagram-set-finishing` G-7이 잡은 `CurvePlot.curves[].tone?`과 같은 유형(시그니처만 있고 구현 없음). W1의 `process-overview`·`fab-cleanroom`이 첫 사용처다 — props 표 정정 또는 구현 중 택일 필요.
- **`ToneStyle.dashed?` 추상화 (선행 L-2 유지)** · **`ARROW_FILL` 상수 (선행 L-3 유지)**: W1에서 새 하드코딩 색은 늘지 않았다(MDX 색 리터럴 0).
- ~~**W1 육안 확인**~~ → ✅ **2026-08-13 종결** (G-9). W1만이 아니라 **사이트 전체 SVG 94페이지를 375px·1440px 전수**로 봤다. 예측 4건 중 ⓐ는 **반증**(밴드 tone은 다크에서 13.2~14.6:1로 문제없음), ⓑ는 **전제 오류**(375px에서 압착이 아니라 카드 목록 재배치), ⓒⓓ는 통과(페이지 가로 넘침 0 · `FlowSteps`는 HTML이라 축소 없음). 실제 결함은 예측 목록에 **없던** SVG 축소·`accent` 대비·`LatticeDiagram` 겹침이었다.
- ~~**W2 착수 시 재사용할 판정**~~ → ✅ 적용 완료. 예측이 맞았다 — 교과서 유형에서 `TreeBranch`+`CompareCards`+`FlowSteps`가 **276/349 = 79.1%**를 차지했고, `daegu-hs-process`는 주제 중복대로 대체 5건이 발생해 `w2-design` §2.7에 기록했다.

## 후속 백로그 (diagram-expansion 파생 · 2026-08-13)

- ~~**렌더 계측을 상시 관문으로 둘지**~~ → ✅ **2026-08-13 완주·아카이브** (`diagram-render-gate`, 100%). **전제가 틀렸다** — "브라우저가 필요해 `verify-diagram-placement.mjs`에 넣을 수 없다"고 적었으나 셋 중 **둘은 브라우저가 필요 없었다.** 실효 글자 크기는 `svgBox()` 코드 계약으로(C-18), 대비는 `theme.css`의 oklch 288색을 sRGB로 변환해(C-19) 정적으로 판정된다 — 타당성을 G-9 브라우저 실측 12쌍으로 증명했다(최대 오차 0.060, 미달 3건까지 재현). 브라우저가 필요한 것은 **좌표가 결정하는 것**뿐이고(무엇이 글자 밑에 오는지·실제 넘침) 그것만 `verify-diagram-render.mjs`로 갔다. 자격 증명 규약은 그대로 지켰다(`process.env`만, `.env.local` 미파싱).
- **`NodeGraph` 간선 라벨 9px**: `fontSize={9}`라 모바일에서 9px·데스크톱 11.7px이다. 키우면 간선 라벨끼리 겹칠 위험이 있어 G-9에서 **판단 후 의도적으로 유지**했다. 겹침 회피 배치를 넣을 때 함께 올린다.
- **`altTable`이 9건뿐**: SVG 109 인스턴스 중 9건만 보유한다. G-9로 좁은 화면에서 **프레임 내 가로 스크롤**이 생겼으므로, 스크롤로 가려지는 도해에 표 대안이 있으면 더 낫다. C-2 표 병치가 원칙이라 대부분은 본문 표가 그 역할을 하지만 전수 대조는 안 됐다.
- **`tone` 13종이 부족하다 (W1 항목 유지)**: 공핍영역·광학층·질화막용 tone이 없다. `depletion` 신설 기준(3모듈 이상)은 이미 넘었다. **신설할 때는 흰 배경이 아니라 `TEXT`·`TEXT_MUTED`가 그 tone 위에 올라간 조합으로 대비를 재야 한다** — G-9가 `accent`에서 확인한 함정이다(tone의 유효성 C-3과 대비는 다른 문제).
- **`FlowSteps.loop.from`/`to` 미해석 (W1 항목 유지)**: `loop.label`만 렌더한다. C-14(없는 prop)를 신설했으나 이것은 **있는 prop이 해석되지 않는** 반대 유형이라 여전히 검사 밖이다.
- **동명 모듈 네임스페이스 규약**: `etch-process`·`diffusion-process`·`process-overview`·`photo-process`가 두 자료원에 동명으로 존재한다. 예외 목록과 `modShape`에서 **같은 버그가 두 번** 났다(내가 한 번, CodeRabbit이 한 번). 모듈을 키로 쓰는 새 자료구조는 처음부터 `자료원/모듈`로 시작한다.

## 후속 백로그 (diagram-render-gate 파생 · 2026-08-13)

- ~~**★`WAVES`에 `first-semiconductor` 편입**~~ → ✅ **2026-08-16 완주·아카이브** (`diagram-gate-coverage`, 98%). 원래 항목: — 브라우저 스윕이 모으는 SVG 92페이지 중 **60페이지**가 C-1~C-17 범위 밖이다. `CLAUDE.md`에 이 사이클이 적은 "도해를 추가·수정하면 `verify:diagram`"이 그 60모듈에는 지켜지지 않는다(C-18·C-19는 컴포넌트·토큰 단위라 무관). 기준선 사이클은 임시 스크립트로 검사했고 그 스크립트는 남지 않았다. **약속과 실제 범위가 어긋나는 건이라 잔존 중 성격이 가장 무겁다.**
- **`verify:render`에 다크 모드 추가** — 지금 `colorScheme: 'light'` 고정이라 D-10이 브라우저에 넘긴 다크 값(`brand-500` 1.40 · `slate-400` 1.00)이 정적·브라우저 두 층 어디에도 걸리지 않는다. 뷰포트 2 × 테마 2면 실행이 약 6분으로 2배가 되므로 D-7을 재판단해야 한다.
- ~~**`LayerStack` 채움 패턴이 층 라벨과 겹친다**~~ → ✅ **2026-08-27 완주·아카이브** (`diagram-label-legibility`, 94%). 원래 항목: — `011-capacitor`·`017-nmos`에서 육안 확인. `+`/`−` 글리프가 층 라벨 글자를 지나간다. 대비가 아니라 **배치** 문제이고 좌표 기반이라 별도 검사가 필요하다. Plan §8 범위 밖이었으나 Do 중 드러났다.
- **`TEXT_MUTED`가 같은 개수로 위치만 옮기는 경우** — D-12 스냅샷은 개수만 본다. `<pattern>` 안은 `checkPatternTextClass()`가 보지만 그 밖의 새 위치라면 사람이 봐야 한다.
- **예외 키 `_component/`·`_token/` 경로가 한 번도 실행되지 않았다** — 등재 0건이라 조립 코드가 검증되지 않았다. 대조군을 붙일지 판단.
- **`prop`이 있는데 콘텐츠가 안 쓰는 경로 검출** (`FlowSteps.loop.from`/`to`) — C-14의 반대 방향. `diagram-expansion` 파생 백로그에서 유지.


## 후속 백로그 (diagram-gate-coverage 파생 · 2026-08-16)

- ~~**★D-10·D-11의 `exit 2` 처분이 미검증**~~ → ✅ **2026-08-24 완주·아카이브** (`diagram-gate-exit-contract`, 97%). 원래 항목: — 대조군이 `hard: false`로만 돌아 "미등록을 **감지**한다"까지만 증명한다. **"감지하면 실패한다"** 는 증명되지 않았고, 그것이 D-10의 결정문 자체다(*"경고가 아니라 `exit 2`"*). 검사기를 자식 프로세스로 띄워 종료 코드를 보는 대조군이 필요하다. **다음 사이클 첫 항목.**
- **`LayerStack` 채움 패턴 글리프와 층 라벨 겹침 (선행 R-3 유지 — 여전히 열려 있다)** — D-13이 고친 것은 **wells와의** 겹침이다. R-3이 든 `017-nmos`는 층 높이 78이라 D-13 로직이 **발동조차 하지 않는다.** 둘을 같은 항목으로 묶어 닫힌 것으로 읽지 말 것.
- **D-13 배치 규칙에 기계 규칙이 없다** — 좌표로 결정되는 겹침이라 정적 검사로 닿지 않고 `verify:render`도 대비·넘침만 본다. **지금 아무 검사도 이것을 지키지 않는다.** `021-mesfet`은 빈 구간 25.96 vs 라벨 폭 26.0으로 **0.04 차**라, `DIM.font`·`GAP_MARGIN`·폭 근사 계수 중 무엇이 바뀌면 라벨이 두 well 사이 좁은 틈으로 들어간다.
- **미사용 예외를 잡는 검사가 없다** — 검사를 느슨하게 바꾸면 기존 예외가 조용히 무효가 된다. *"제외의 근거는 검사보다 약하다"* 가 정작 예외 자체에는 적용되지 않고 있다. 예외 59건 중 몇이 이미 죽었는지 아무도 모른다.
- **`assertGlobalIdPrefix`가 `split()`을 거치지 않는다** — `idPrefix` 수집이 자식 영역까지 훑어 D-9의 "제3의 범주" 원칙과 어긋난다. 현 콘텐츠 영향 0건(자식 안에 `idPrefix`가 없다)이라 결함이 아니라 원칙 위반이다.
- **`--wave w4` 단독 호출이 `_coverage: 'selective'`를 놓친다** — C-1 74건을 낸다(`--all`은 정상). HEAD에서도 동일한 기존 문제. 웨이브 단독 실행과 `--all`이 다른 판정을 내는 것 자체가 결함이다.
- **`verify:render`에 다크 모드 (선행 R-2 유지)** — `colorScheme: 'light'` 고정.

## 후속 백로그 (diagram-gate-exit-contract 파생 · 2026-08-24)

- **★G-7 미충족 — `--all` 1.72초, 손자 40개** — 대조군 8건이 웨이브마다 돈다. `--all` 부모는 `selfTest`를 돌지 않고 자식 5개를 띄우므로 같은 사실 8개가 40번 증명된다. 중복 제거에는 *"자식이 자기가 자식임을 아는"* 이음매가 필요하고, 어떤 형태(환경변수·내부 플래그)든 **잘못 쓰면 대조군을 건너뛴다.** **이음매 설계 문제이지 대조군을 더해 풀 문제가 아니다.**
- **`--all --assert-only`가 침묵으로 무시된다** — `--all` 분기가 먼저 걸리고, 자식에 인자를 전달하지도 않는다. **싼 경로를 요청한 사람이 5웨이브 전수 검사를 받는다.** 오류도 경고도 없다. 부수로 인자 검증이 `--assert-only` 종료보다 뒤라 `--assert-only --wave=bogus`는 조용히 `exit 0`이다.
- **남은 `exit 2` 경로** — 배치표 부재 · C-19 해석 실패 · 인자 오류 3종. **하네스가 이미 있어 후속은 대조군 한 건씩이다**(G-7과 혼동하지 말 것 — 그쪽은 이음매 설계다).
- **`LINKS`의 `docs`가 죽은 코드** — 빼도 자체검사 출력이 변하지 않는다(`ALLOW_PATH`를 읽는 것은 `--assert-only` 종료보다 뒤다). 지금 빼면 그 경로가 생겼을 때 조용히 깨진다.
- **스크립트 헤더 검사 목록이 표류한다** — C-11~C-15·C-18·C-19가 빠져 있는데 `CLAUDE.md`는 C-1~C-19라 적는다. 선행 표류.
- **선행 `diagram-gate-coverage` 파생 백로그 유지** — R-3(패턴 글리프 겹침) · D-13 배치 규칙의 기계 규칙 · 미사용 예외 검사 · `--wave w4` 단독 호출의 `_coverage` 누락.

## 후속 백로그 (diagram-label-legibility 파생 · 2026-08-27)

- **★C-2 — 106건을 낸 검사기 결함에 대조군이 없다.** `verify:render`가 `defs`·`mask` 안의 도형을 배경으로 읽던 것을 고쳤는데, 그 스크립트에는 **자체검사 하네스가 없다.** 이 저장소의 규약("검사기를 고치면 대조군을 남긴다")의 예외로 남았다 — **되돌아가도 아무도 모른다.** 하네스 신설이 별개 사이클감이다.
- **B-1 2줄 라벨이 층 밖으로 나간다** — `labelY`가 `wells` 유무로 갈리는데, wells가 있으면 2줄째 baseline이 **항상 층 밖**이고 없어도 `h < 44`면 나간다(기본값 40이 여기 걸린다). 층 밖에는 그 라벨을 위한 구멍이 없다. **잠복** — 현재 `LayerStack` 라벨에 `\n` 0건이나 `usage.md` §3.2가 "12자 넘으면 `\n`"을 권한다. 브라우저 규칙이 잡는다.
- **B-3 wells 구멍이 well을 통째로 삼킬 수 있다** — 구멍 폭이 최대 `wLabelW + 39`이고 `wellW`는 주석이 있으면 113이다. 한글 7자면 넘어 그 well의 패턴이 전부 사라진다 = 색각 이상 대비 장치 소멸. 여유 **한글 약 3자**.
- **`lastFile` 절대경로가 매 사이클 재생성된다** — bkit이 자동 생성하는 필드다. 세 사이클 연속 CodeRabbit이 잡았고 세 번 다 손으로 고쳤다. 커밋 전 훅이나 `.gitignore` 분리 같은 구조적 해법이 필요하다.
- **`⊤` 세로로 잘린 글리프** — 층 rect 높이가 타일 글리프를 자른다(`patternUnits="userSpaceOnUse"`라 타일 격자가 층 경계에 정렬되지 않는다). 이 사이클이 만든 것이 아님을 마스크 전 사진으로 확인했다.
- **선행 백로그 유지** — G-7 중복 실행 이음매 · `--all --assert-only` 침묵 무시 · 미사용 예외 검사 · `--wave w4` 단독 호출의 `_coverage` 누락 · D-1 `LayerStack` 독스트링 모듈 수 표류.