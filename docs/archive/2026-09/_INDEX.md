# Archive Index — 2026-09

| Feature | Archived | Match Rate | Iter | 시간 | Note |
|---------|:--------:|:---:|:---:|:---:|------|
| [render-gate-self-test-denominator](./render-gate-self-test-denominator/) | 2026-09-01 | 91% | 3 | 3일(다세션) | 선행 `render-gate-derivation-claim` 잔존 **D-2** — *자체검사가 대조군 40건을 돌리는데 그 수를 아무도 안 잰다.* 착수 실측: 하나를 지우니 **39건인데 `✅ 전 항목 통과 · exit 0`**(선행 사이클이 이미 대가를 치렀다 — 35→36이 됐는데 아무도 안 울었고 `CLAUDE.md`가 **두 사이클** 표류). 무리별 세 수(판정 14·범위 13·처분 13)를 선언하고 **실행 집계**와 `!==`로 대조 → 어긋나면 `exit 2`. **한 수가 아니라 세 수인 이유**는 무리 간 이동(총계 40 그대로)을 한 수로는 못 보기 때문이고, 그 경우를 되돌림으로 확인했다. ★**이 사이클의 값은 네 겹을 판 데 있다** — ① `fixtures.length`를 "실행 집계"라 불렀는데 **배열 길이**였다(루프 안 `continue` 한 줄이면 39줄만 찍히는데 집계는 14·exit 0), ② 단언은 고쳤는데 **출력**이 6줄 아래에서 죽은 값을 계속 찍었고 `disposeRan`은 판정이 아니라 **호출**을 셌다, ③ 감사 가드를 1→3행으로 늘렸으나 사유 정규식이 셋 다 같아 **셋인 척하는 하나**였다, ④ 새로 세운 프로세스 경계 핸들러·무리 이름 대조가 **둘 다 무대조군**이라 지워도 전 항목 초록이었다. ★★**판독자 처방을 넣고 재 봤더니 안 나았다** — 2차 판독이 오배선 구멍(`['처분', scopeRan, DISPOSE_CONTROLS]`)을 정확히 짚고 *"사유 정규식을 무리별로 좁혀라"* 를 처방했는데, 넣고 탐침을 돌리니 여전히 가드 초록이었다: 정규식은 tally 행의 **라벨**을 볼 뿐 그 행이 **어느 계수기를 읽는지**는 못 보고, 라벨은 오배선해도 그대로다. → **재는 대신 없앴다** — 선언·집계를 키 있는 객체(`CONTROLS`/`ran`)로 묶고 tally를 `Object.entries`로 유도해 **손으로 짝짓는 자리를 지웠다**(오배선이 *쓸 수 없는 모양*이 된다). 백로그 F-2(`범위`·`처분`이 둘 다 13이라 맞바꿔도 조용)도 같은 뿌리라 함께 죽었다. 종료 코드 계약도 닫았다 — 검사기가 터지면 Node 기본값 **1**(=계약상 *콘텐츠 위반*)로 새서 `--all` 부모가 지나쳤는데, 안쪽 `try`(G-3)에 더해 **프로세스 경계**(`uncaughtException`/`unhandledRejection`)에서 `2`로 정규화해 `try` 밖 넷(fixture 생성·브라우저 기동·`close()`·호출부)을 덮었다. **문서-코드 미러는 기계가 잰다** — `CLAUDE.md`의 감사 수치가 **같은 사이클 안에서 두 번**(가드 1→2, 2→4) 어긋난 뒤, 감사가 앵커 줄(`현재 **배선 …**`)을 읽어 자기 실측과 대조하고 어긋나면 `exit 2`를 낸다(못 읽거나 앵커가 없어도 `die` — 초판의 `catch { doc = null }`은 **조용히 건너뛰고 ✅**를 찍었다). 찍는 문자열과 재는 문자열을 `FIGURES` 하나로 묶어 **두 번 포맷**(`(+ 가드` vs `(+가드`)하던 새 미러도 없앴다. 감사 앵커는 `declOf()`로 소스에서 유도(주석까지 베낀 전체 줄 리터럴 폐기·중복 앵커도 `die`), 소요는 감사가 스스로 찍는다. **실측** — 자체검사 40/40 `판정 14 · 범위 13 · 처분 13 — 선언과 일치`, 감사 **배선 12/12 · 규칙 5/5(+가드 1/1 → 7/7)** · 항목 24 · 422.9초 · 원복·고아 확인, `typecheck`·`lint` 0, `verify:diagram` 5웨이브. **되돌림 15건 전량 관측** — 선언 셋을 각각 흔들면 그 무리만 운다 / **오배선은 훼손 하나로** 운다(처분 0·범위 26) / 오배선+선언 동시(2차 판독이 지목한 조합)에도 운다 / `ran`에만 무리 추가 / 판정 대조군 `continue` 건너뛰기(G-2 회귀) / 더하기 / 무리 간 이동 / `try` 안팎 예외 / **핸들러를 빼면 `2→1`** · **이름 가드를 무력화하면 `2→0`**(가드가 자기 장치를 재는지까지 관측) / 문서 수치·앵커. 독립 판독 **4회**(88 → 88 → 89 → **91**) — 매 판독이 한 겹 아래를 찾았고, 3차까지는 *"고쳤다고 주장했는데 안 고쳐진 것"* 이 나왔다. 3파일 **+295/−16**, 앱 코드 무변경. **상한을 코드에 적었다** — 문서 대조의 앵커 줄 좁히기가 이 감사의 유일한 무대조군(판별 되돌림이 1건당 7분 → F-8), ⓐ 가드는 핸들러의 반쪽만 잰다(F-9), 소요는 단언 없이 찍기만 한다. *"덮지 않은 것을 말하지 않으면 이 수치가 닫았다로 읽힌다."* |
| [doc-link-gate](./doc-link-gate/) | 2026-09-03 | 87% | 7 | 2일 | 선행 `render-gate-self-test-denominator` 파생 **F-10** — *문서 링크를 아무도 안 잰다.* 도해에는 관문이 다섯 겹인데 문서 링크는 무방비였고, CodeRabbit이 죽은 링크 1건을 짚었을 때 세니 **21건**이었다. `npm run verify:links` 신설(683줄) — `.md`·`.mdx` 전량에서 마크다운 링크·이미지와 **JSX/HTML의 `src=""`·`href=""`** 를 **파서로**(`remark-parse`+`remark-gfm`) 뽑아 대상 실재를 본다. ★**정규식이 아닌 이유는 실측이다** — 사고 링크 후보를 정규식으로 찾으면 넷인데 파서가 인정한 것은 하나였다(나머지는 목적지에 공백이 있어 CommonMark상 링크가 아니다). **거짓 경보투성이 관문은 곧 무시된다.** `remark-mdx`는 안 쓴다(`.mdx`에서 링크 집합 차이가 **0**인데 `.md` 여럿을 파싱 실패시킨다) — 그 대신 JSX가 `html` 노드로 와서 **속성을 볼 수 있게 된다**. ★**allow 파일을 일부러 안 만들었다** — 필요가 **0으로 측정**됐다(파서가 코드 스팬을 건너뛰므로 *문서가 링크를 설명하는 자리*는 애초에 링크가 아니다). 라우트는 `src/app`의 `page.*`·`route.*`에서 **유도**하고 동적 세그먼트는 해석기 등록제 — **해석기 없는 동적 라우트로 가는 링크는 조용히 통과시키지 않고 `exit 2`**(렌더 관문의 δ와 같은 처분), 그리고 **미등록 템플릿 7종을 스스로 말한다**. 종료 코드 계약 `0`/`1`/`2` 준수. **살아 있는 결함 1건을 실제로 고쳤다** — `1[H](헨리)`가 하이퍼링크로 렌더되고 있었고, 그 규칙(*"대괄호 직후 여는 괄호는 링크 문법"*)은 2026-07 설계서에 **적혀 있었다**: *적혀 있었고, 지켜지지 않았고, 아무도 몰랐다.* **덮는 범위 0 → 607건**(`link 177 · image 4 · src 425 · href 1`) · 대조군 **43건**(판정 29 · 처분 14, 개수와 **식별자 유일성·무리**를 스스로 단언) · **되돌림 26건 관측**. ★★**독립 판독 일곱 번**(72 · 82 · 84 · **80↓** · 86 · 84 · 87)이 매번 **한 겹 아래의 무대조군**을 짚었다: 출력의 *"안 보는 것"* 문구 → `resolvePublic` **배선** → 다중행 JSX **형태** → `settled` 삼항(손으로 지은 짝) → 상한을 **적은 그 문구** → `seenNames` **배선** → **설계서**. *순수 함수를 뽑으면 부르는 자리가 무대조군이 되고, 장치를 세우면 그 장치가, 문구를 적으면 그 문구가 무대조군이 된다.* **4차의 하락(84→80)이 이 사이클의 가장 정직한 수다** — 3차 점수가 *"12건 반영했다"* 는 **주장**을 전제로 매겨졌는데 셋이 안 들어갔다. ★**판독의 처방을 넣고 재 봤더니 안 먹은 것이 네 번**이고, 대표는 6차다: *"`ran += 1`과 `push()`가 두 문장이라 조용하다"* 는 진단은 옳았는데 처방(**헬퍼로 묶기**)을 넣어도 **여전히 두 문장이라 그대로 조용했다**(실측 `exit 0`). **원천을 하나로**(이름 배열의 길이가 곧 수) 만들고서야 물었다 — *묶는다고 하나가 되는 것이 아니다.* 또 *"이미지도 본다"* 가 실물의 극히 일부였는데(마크다운 `![]()`만 봤고 실제는 다중행 `<ImageFigure src=`), 처방은 *"안 보는 것에 적어라"* 였지만 재 보니 **거짓 경보 0**이라 **문구로 덮지 않고 실제로 봤다**(범위 3.4배). 그리고 *"대조군을 만들 수 없는 자리"* 수를 넷→다섯→여섯→일곱으로 **다섯 번 틀린 뒤**(실제는 열) 수를 지우고 목록만 남기기로 했다(⚠️ **그 편집은 안 들어갔고** — 배치의 뒤 항목이 죽어 쓰기가 통째로 안 됐는데 ✅만 보고 넘어갔다 — 후속 사이클이 잡아 실제로 지웠다. **성공 메시지는 성공이 아니다**) — *손으로 세는 수는 다음에도 틀린다.* 그 목록에서 가장 무거운 것을 이름으로 적었다: **본 검사 앞 게이트**(`!DLG_CHILD && !await runSelfTest()`) — 그 배선이 죽으면 자체검사 실패가 본 검사를 못 막고 **대조군 전부가 장식**이 된다. **아카이브 자체가 관문의 첫 실전 발화였다** — 문서를 옮기자 형제 링크 7건이 죽었고 `exit 1`로 잡혔다(F-10에서 사람이 손으로 세던 그 일이다). `typecheck` 0 · `lint` Error 0 · `verify:diagram` 5웨이브 · 앱 코드는 콘텐츠 한 줄 외 무변경. |
| [doc-link-route-resolvers](./doc-link-route-resolvers/) | 2026-09-05 | 85% | 3 | 1일 | 선행 `doc-link-gate` 파생 **F-14 닫힘** — 관문이 동적 라우트 **7종을 미등록으로 선언**하고 그리로 가는 링크에 `exit 2`를 냈다. 정직한 처분이지만 **지뢰**였다: 착수 실측에서 `[1장](/chapter/…)` 한 줄이면 곧바로 `exit 2`인데 **저자가 가장 걸 법한 것이 챕터 링크**다. `.mjs`가 TS를 못 읽는다는 이유로 **세 번째 수동 미러**를 만드는 대신(`CLAUDE.md`가 명시적으로 경계하는 것) **`tsx`로 앱의 진짜 `src/lib/sources.ts`를 그대로 평가**한다 — 앱이 `generateStaticParams`에서 쓰는 **바로 그 함수**를 부른다(0.35초 · 절대 경로로 **한 번만·필요할 때만**). 해석기 **1종 → 8종**, 동적 전개 **375**(JSON 3종 56 · TS 5종 319). ★★**이 사이클의 값은 지뢰를 없애는 일이 거짓말을 만들 뻔한 것을 1차 판독이 잡은 데 있다** — 비전역 `replace` 하나로 `/sources/[source]/[module]` + `'src-a/mod-a'` → `/sources/src-a/mod-a/[module]`, **206건 전량이 쓰레기 문자열**이었다. 진짜 링크가 집합에 없으니 정직한 `exit 2`가 **거짓 `exit 1`**(*"라우트가 `src/app`에 없다"*)로 바뀐다 — **저자에게 없는 잘못을 말하는 관문은 시작보다 나쁘다.** 셋이 겹쳐 조용했다: `expanded`가 `values.length`를 찍어 출력엔 `→ 206`이 **정직하게** 나오고 · 실제 그런 링크가 0건이라 본 검사도 초록이고 · 대조군 `D-17`이 **브래킷 하나짜리** 템플릿을 써서 못 봤다(**fixture의 모양이 판별력을 정한다**). → 세그먼트 **조립**(치환이 아니다) + **개수 단언** + `D-20`·`D-21`. ★**같은 실패가 세 번, 원인은 매번 달랐다** — `exit 2` 계약 목록이 세 번 낡았는데 1·2차는 **편집 배치의 뒤 항목이 죽어 쓰기가 통째로 안 됐고**(✅만 보고 넘어갔다 — 선행 아카이브의 *"수를 지우고 목록만 남겼다"* 가 거짓이 된 그 실패다. 도구를 편집마다 **즉시 쓰고 다시 읽게** 고쳤다), 3차는 **쓰기는 성공했고 손으로 센 열거가 불완전**했다(같은 배치가 만든 자리 둘을 빠뜨렸고 *"`fail()`이 가는 자리 전부"* 라 적었는데 절반은 `fail()`이 아니었다). **도구를 고쳐도 손 열거는 안 고쳐진다** → 열거를 지우고 `rg 'fail\(\|process\.exit\(2\)'` 로 **강등**했다. ★**장치를 늘리면 옛 장치가 무력해진다** — 해석기가 1→8이 되자 하나일 때 눈에 보이던 것이 안 보였다: 1차 *해석기 4가 무대조군* → 2차 *osha↔ncs↔daegu를 맞바꿔도 조용 · 키↔트리 대응이 사라짐* → 3차 *`DLG_KEYCHECK` 분지의 **배선***. Design 단계 **D-0**이 실측으로 먼저 막은 것도 같은 모양이다 — 대조군 fixture 둘이 **실제 템플릿 이름**을 빌려 써서 그 라우트를 등록하는 순간 조용히 무너질 참이었다(→ **합성 템플릿** `합성/[없는것]`·`합성둘/[없는것]`이 영구 표적). **실측** — 대조군 **55**(판정 29 · 처분 26, 착수 40에서 +15) · 되돌림 **8/8**(1차 Act 4 · 2차 Act 4) · 본 검사 `문서 573 · 참조 614 · 소요 6.08초 · exit 0` · `typecheck` 0 · `lint` Error 0 · `verify:diagram` 5웨이브 · 앱 코드 무변경(`.mjs` +335 · `CLAUDE.md` +18). 판독 3회(62 → 80 → 85). ⚠️ **90% 미만에서 닫는다** — 남은 15는 대조군을 하나 더 쓰면 되는 종류가 아니라 **검사기가 자기를 검사하는 구조 자체**가 막는다(`!DLG_CHILD` 조건 둘은 자식을 그 환경변수 없이 돌리면 **재귀**한다 · `TS_PROBE`는 앱 본문의 필사본). 억지 대조군을 세워 수를 만들지 않고 **F-12(감사)·F-24**로 넘겼고, 그 판단 근거를 보고서 §4에 적었다. **아카이브가 또 관문의 실전 발화였다** — 문서를 옮기자 형제 링크 **4건**이 죽었고 `exit 1`로 잡혔다(선행 사이클은 7건). |

---

## 이 사이클이 남기는 문장

> **"실행 집계"라고 부른다고 실행을 세는 것이 아니다.**
> 배열 길이는 배열이 몇 개인지를 말하지, **몇 개가 돌았는지**를 말하지 않는다.
>
> **행을 셋으로 늘렸다고 셋을 재는 것이 아니다.** 사유 정규식이 셋 다 같으면 셋인 척하는 하나다.
>
> **판독자가 준 처방을 넣었다고 병이 낫는 것이 아니다.**
> 넣고 재 봤더니 안 나았다 — 처방이 겨눈 것은 라벨이었고 병은 배선에 있었다.
>
> **장치를 세웠다고 그 장치가 지켜지는 것이 아니다.**
> 이번에 세운 셋 중 둘은 지워도 아무도 울지 않았다.

**재서 막는 것과 못 쓰게 막는 것은 다르다.** 이 저장소는 오래 전자를 해 왔고,
그때마다 *"재는 자가 옳게 재는가"* 라는 한 겹을 새로 떠안았다. 이번엔 후자가 쌌다.

### 다음 사이클로 넘기는 습관

네 판독이 매번 **같은 모양**을 잡았다 — 고친 문장과 똑같은 문장이 옆 블록·옆 파일에 하나 더 있었다.

| 판독 | 고친 곳 | 남아 있던 쌍둥이 |
|:--:|---|---|
| 2차 | 단언의 `fixtures.length` | **출력**의 `fixtures.length`(6줄 아래) |
| 3차 | `CLAUDE.md`의 예시 손 숫자 | 감사가 **매 실행 찍는** `10건` · 관문 주석의 `3종` |
| 4차 | `CLAUDE.md`의 `14·12·14` | **관문 주석**의 `14·12·14` · `489`가 표와 본문 두 곳 |

> **고칠 때 그 문자열이 몇 곳에 있는지 먼저 센다.**

---

## 후속 백로그 (render-gate-self-test-denominator 파생 · 2026-09-01)

- **★F-1 정적 관문의 분모가 무방비** — `verify-diagram-placement.mjs`의 `대조군 69건`·`8건`이 손 숫자이고 지키는 것이 없다(현재 값은 실측으로 맞다). `CLAUDE.md`가 **두 정책을 동시에 주장한다**: 렌더 관문은 *"수는 코드가 단언한다"* 인데 정적 관문은 손 숫자다. 이 사이클이 렌더 쪽에 세운 처방(`CONTROLS` 객체 + 실행 집계 대조)을 그대로 옮길 수 있다.
- **★F-7 거짓 면책** — `collectUrls()`의 `catch`가 위반 1건을 **지어내** `repoBroken`을 켠다. 그러면 실제 트리를 쓰는 ⑳·㉛이 *"이 대조군의 잘못이 아니다"* 면책을 찍는다. **거짓 면책 = 거짓 초록**이라 이 사이클의 병 계열이고, 같은 파일이 *"거짓 면책을 없앴다"* 고 적은 자리의 **좁은 판본**이 남아 있는 것이다.
- **F-6 G-2 상비화 — ★훼손의 모양이 중요하다.** `fixtures.slice(1)`은 *길이 계수*에서도 `13 ≠ 14`로 울어 **두 구현을 못 가른다.** 판별자는 **루프 안 `continue` 주입**이다(되돌림 #7의 모양). 감사 자신이 적어 둔 *"훼손의 모양을 재 보지 않으면 엉뚱한 것을 건다"* 가 이 백로그 문구에 그대로 적용된다 — 문구를 안 고쳐 두면 다음 손이 헛돈다.
- **F-8 문서 검사를 `--doc-only`로 분리** — 지금 `CLAUDE.md` 대조는 감사 끝에만 있어 **되돌림 1건당 7분**이다. 그래서 `doc` → `line` 좁히기가 이 감사의 **유일한 무대조군**으로 남았다(판별 되돌림: *"앵커 줄은 낡았는데 같은 수치가 문서 다른 곳에 또 있다"*). 분리하면 초 단위가 되고 그 자리가 닫힌다.
- **F-9 ⓐ 가드가 핸들러의 반쪽만 잰다** — 훼손이 async 안 throw라 `unhandledRejection`만 탄다. `process.on('uncaughtException', …)` 등록 줄만 지우면 가드는 초록이다.
- **F-3 오도하는 실패 메시지(셋)** — ① 정당하게 선언을 올린 사람이 받는 *"규칙 앵커가 사라졌다"*, ② 무리 이름 가드가 `join(',')`이라 **재정렬만 해도** *"한쪽만 고쳤다"* 로 죽는다, ③ `wantReason` 누락 시 `TypeError`가 *"자체검사가 예외로 죽었다"* 로만 보고돼 원인을 안 가리킨다.
- **★F-10 문서 링크를 아무도 안 잰다** — 이 사이클이 남긴 문장(*"고칠 때 그 문자열이 몇 곳에 있는지 먼저 센다"*)이 **자기 아카이브에 그대로 걸린 자리**다. CodeRabbit은 상태 파일(`planDoc`·`designDoc`) 하나를 짚었는데, 저장소를 세니 **21건**이었다.

  | 성질 | 건수 | 예 |
  |---|:--:|---|
  | 아카이브로 옮겨져 형제가 됐는데 링크가 옛 자리를 가리킨다 | 15 | `../../01-plan/features/x.plan.md` → `./plan.md` |
  | 자기 위치를 두 번 셌다(`archive/` 중복) | 4 | `../../archive/2026-05/y/` → `../y/` |
  | 깊이가 한 칸 모자라다(**대상은 멀쩡히 있다**) | 1 | `../../../src/…/Tag.tsx` → `../../../../src/…/Tag.tsx` |
  | ★**살아 있는 문서**가 아카이브된 문서를 옛 자리로 가리킨다 | 1 | `docs/04-report/ch3-ch4-foundation.report.md:13` |

  **전건 수정 완료**(2026-05 19 · 2026-08 1 · `docs/04-report/` 1). 링크 텍스트가 이제 거짓인 곳(`docs/01-plan/features/…`)은 텍스트도 함께 고쳤다. 저장소 `.md` **224개 재스캔 0건.**

  > ★**수가 아니라 판정 근거가 빠져 있었다**(후속 `doc-link-gate` Design §0.0). 위 *"0건"* 은
  > **정규식 스캐너**로 잰 값이고, 그 스캐너는 곧이어 이 표의 예시 넷(`` `../../01-plan/…/x.plan.md` `` 따위)을
  > *죽은 링크*라고 울었다. **파서(`remark-parse`)에 물으면 그 넷은 링크가 아니다** — 전부 코드 스팬 안이라
  > CommonMark가 링크로 안 본다. 즉 **0건은 참이었고, 그것을 뒤집었다고 본 경보가 거짓이었다.**
  > 틀린 것은 문장이 아니라 **그 문장을 재던 자**다. 지금 값은 파서 기준으로도 **0건**이다
  > (파일 후보 115건 · 라우트 23건 · 해석 실패 1건은 링크가 아니라 **사고 링크**다 — 아래 후속 사이클이 다룬다).

  **남은 것은 재는 자였다** — 후속 사이클 `doc-link-gate`가 `npm run verify:links`로 그것을 세웠다(F-10 닫힘). 도해에는 관문이 다섯 겹인데 **문서 링크는 무방비였고**, 그래서 셋이 각각 다른 이유로 몇 달을 살아남았다. 다음 사이클감: ① 아카이브 절차에 *"옮긴 문서의 형제 링크는 `./`로"* 규칙 한 줄, 또는 ② **링크 해석 검사를 관문에 추가**(도해 관문과 같은 계약 — 못 풀면 `exit 2`). ②가 낫다 — 아카이브만이 링크를 깨는 것이 아님을 위 표의 셋째·넷째 줄이 보여 준다.
- **D-7 감사 자신을 재는 자** — 이 사이클도 범위 밖으로 두었다. 한 겹 더 쌓는 일이라 값을 따로 따져야 한다.
- **선행 백로그 유지** — E-3 · A-3 잔여 · D-1 · D-3 · D-4 · D-6 · D-9~D-13, 그리고 `docs/archive/2026-08/_INDEX.md`의 후속 백로그 전량(★C-2 `verify:render` 하네스 부재 · G-7 중복 실행 이음매 · `--all --assert-only` 침묵 무시 · `--wave w4` 단독 호출의 `_coverage` 누락 · `lastFile` 절대경로 재생성 · B-1/B-3 잠복 결함).

## 후속 백로그 (doc-link-route-resolvers 파생 · 2026-09-05)

- **★F-12 대조군을 만들 수 없는 자리** — `verify:links`가 자기를 검사하는 구조 때문에 `!DLG_CHILD`로 조건 지어진 둘(**본 검사 앞 게이트** · **죽은 해석기 키 검사의 `!DLG_CHILD ||` 분지**)이 **구조적으로 관측 불가**다. 자식을 그 환경변수 없이 돌리면 자체검사가 **재귀**한다. 도해 관문의 `audit:wiring`과 같은 꼴의 **감사**가 있어야 닫힌다 — 그리고 그 감사는 다시 무대조군이 된다(D-7과 같은 물음).
- **F-24 `!DLG_CHILD` 조건을 없애는 안** — `okTree`가 기본으로 8종 라우트 디렉터리와 가짜 TS를 소유하게 만들면 자식도 자체검사를 돌 수 있다. 대신 **자식 26개가 전부 `tsx`를 띄운다** — 비용을 재고 정할 일이지, 재기 전에 만들 것이 아니다.
- **F-25 빈 세그먼트** — 해석기가 `'a/'`를 내면 개수 단언(`split('/').length`)을 **통과**하고 `/sources/a/` 같은 매칭 불가 라우트를 조용히 만든다. 실물 0건이라 가드를 안 세우고 **코드 주석에 적었다**(*"필요를 재지 않고 장치를 만들지 않는다"*).
- **F-26 *"새 해석기마다 대조군"* 을 강제하는 자가 없다** — FR-6은 사람이 지킨다. `D-24`(해석기 키가 전부 트리에 있으면 `exit 0`)가 **주의**만 강제한다: 해석기를 더하고 `ALL_ROUTE_DIRS`를 안 고치면 빨개진다. 그러나 *대조군을 안 세운 것* 자체는 여전히 조용하다.
- **F-17 catch-all `[...x]` — 성질이 바뀌었다.** 미등록이면 **여전히 거짓 위반**이고, 등록하면 새로 생긴 개수 단언이 `exit 2`를 낸다. 실물 0건이라 미룬다.
- **★F-27 표가 깨지는 것을 아무도 안 잰다** — CodeRabbit PR #46이 짚었고 **실측이 맞았다**: GFM 표 안의
  리터럴 `\|`는 **백틱 안이라도** 열 구분자로 파싱된다(파이프 이스케이프가 인라인 파싱 *앞*에 온다).
  이 사이클이 3건을 새로 만들었고(`\|` 이스케이프로 해소) 저장소 전량을 재니 **데이터가 잘리는 행이
  4건 더** 있다 — `2026-05/filter-button-promotion/report.md:234~236`(열3→셀4) ·
  `2026-07/chapters-source-fidelity/design.md:101`(열3→셀6). **`1[H](헨리)`와 같은 계열**이다:
  마크다운이 적힌 것과 다르게 렌더되는데 아무도 안 본다. `verify:links`는 **링크 모양**만 보고
  **표 모양**은 안 본다. 재는 자를 세우면 그 자리가 닫힌다(파서는 이미 있다 — `table` 노드의
  헤더 셀 수와 각 행을 대조하면 끝이다). 셀이 **모자란** 행 11건은 빈 칸 렌더라 성질이 다르다.
- **★F-28 필사본을 없앨 수 있다 — 재보지 않고 "전부"라고 적었다** — CodeRabbit PR #46 재리뷰(Major)가
  `TS_PROBE`의 앱 본문 복사를 짚었다. 그 자리는 이미 상한으로 **세 곳에 적혀 있었지만**, 설계의
  *"적어 두는 것이 지금 할 수 있는 전부다"* 는 **거짓이었다**: 앱 `page.tsx`의 `generateStaticParams`를
  `tsx`로 **직접 부를 수 있다**(실측 — `[source]/[module]` 206 · `chapter/[slug]` 17, 프로브 수와 일치).
  가짜 트리에는 `page.tsx`가 없으니 프로브가 남지만, **본 검사에서는 앱을 부르고** 가짜 트리에서만
  프로브를 쓰거나 **둘을 대조해 어긋나면 `exit 2`** 를 낼 수 있다. 후자면 필사본이 낡는 순간 관문이 운다.
  *상한으로 적는 것이 재보는 것을 대신하지 않는다.*
- **선행 백로그 유지** — F-11(exit-2 분기) · F-13 · F-15 · F-18~F-22.
